"""Local tools for the Tier 2 tool loop (spec: docs/specs/2026-08-26-
jarvis-personal-assistant.md, section 4). Every module here exposes
``make_tools(cfg, services) -> list[ToolSpec]`` and nothing else is
imported by the brain."""
