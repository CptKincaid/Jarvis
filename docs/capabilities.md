# Jarvis capabilities

One row per capability. Status: live / verified-offline / experimental.

| capability | how to invoke (voice/typed phrasing) | module | status | notes |
|---|---|---|---|---|
| Film-JARVIS persona (voice for every spoken reply) | Any conversational phrase that reaches the brain, e.g. "Jarvis, how are you?", "tell me a joke", "I'm bored", "give me a status report", "what can you do", "appreciate it, Jarvis", "good night"; also colours Tier 3 [SPEAK]/[DONE] lines and autonomous-task updates | jarvis/brain.py — VOICE_RULES, FEW_SHOT_PINNED / FEW_SHOT_POOL / FEW_SHOTS, select_few_shots(), JARVIS_SYSTEM, CLAUDE_SYSTEM, AUTONOMOUS_PROMPT, build_ollama_system() | live (Tier 2 llama3.2; verified offline + against Ollama) | Dry British understatement, "sir" most of the time, "Hunter" occasionally; one short sentence is the norm, never more than two; no lists/markdown/emoji; admits limits ("I'm afraid...") and never claims a check or action it did not do. The context is labelled background and is not recited; a status report is the git line in his own words with a dry aside, never a raw readout. Reasons are one concrete picture about him or the hour, never a principle (textbook words such as "complexity", "ensure", "can cause" are banned); jokes are dry remarks about his situation, never riddles or set-ups. Few-shots: three pinned lines ("you there" and a thank-you after the greeting, good night last) plus 11 situation families (greeting, small talk, status, joke, wit, no-data, cannot-act, capability, bad idea, mistake, advice) with 2-3 variants each and differing openers; one variant per family is sampled per call (process-seeded RNG, pin with JARVIS_SHOT_SEED). The rules the 3B model only obeys from the closing line live there: answer in your own words keeping the manner of the examples, call him sir, a joke is never a question and its answer, a status answer has no numbers (round-3 A/B over 66 prompts: riddles 3/12 to 0/12, number readouts 5/12 to 1/12, verbatim copies 9 to 4, "sir" 74% to 88%). The rules quote nothing longer than a phrase: a full sentence quoted inside a rule is copied into every situation (guarded by tests/test_persona.py). Tests: tests/test_persona.py (live check: JARVIS_LIVE_OLLAMA=1). Samples: scratchpad persona_baseline.md, persona_round1.md, persona_round2.md, persona_round3a.md / 3b.md (intermediate), persona_samples.md (final; harness persona_eval.py, flags PARROT / NEAR-PARROT / RIDDLE; A/B scripts persona_iter2.py, persona_iter3.py). Commander's canned replies (commander.py) still use the older wording. |
| Spoken-reply guards (Tier 2 -> TTS) | Automatic on every Ollama reply | jarvis/brain.py — spoken_from_ollama(): clean_ollama_reply(), guard_clock_claims(), limit_sentences(); trim_spoken() in _parse_response | live (verified offline) | Strips "Jarvis:" labels, run-on "User:" turns, stage directions, bullets, emoji and file extensions ("tts.py" is spoken "tts"); drops any invented hh:mm clock reading when the context has no "Current time" line (falls back to an honest "no clock" line); caps replies at two sentences in code; free text is trimmed at sentence/clause boundaries, never mid-word, with one hard limit shared with TTS (500). Ollama stop strings end run-on transcripts. |
| Courtesies at Tier 1 | Whole-utterance "are you there / you there / can you hear me", "thank you / thanks / cheers / appreciate it / much appreciated / nice one / good job / well done / spot on / brilliant", "good night / night / off to bed", with or without "Jarvis" (jarvis mode, or with the prefix) | jarvis/commander.py — courtesy_kind(), courtesy_reply(), COURTESY_REPLIES, _h_courtesy; REGISTRY entry "courtesy" and the _route_text pre-brain check | live (verified offline) | Answered instantly in voice with a little variation ("Always, sir." / "Not at all, sir." / "Good night, sir. I'll be here."), spoken regardless of talk-back, because the 3B model muddles short "..., Jarvis" phrases (round-3 samples: "Nice one, Jarvis." was read as sarcasm); other paraphrases ("are you still with me?") still go to the brain, where the pinned thank-you few-shot covers them. Tests: tests/test_commander.py (Tier 1 courtesy section). |
| Clock questions at Tier 1 | "what time is it", "what's the time", "have you got the time", "what's the date (today)", "what day is it" — with or without the "jarvis" prefix (jarvis mode) | jarvis/commander.py — clock_kind(), clock_reply(), _h_clock; REGISTRY entry "clock" and the _route_text pre-brain check | live (verified offline) | Answered from datetime.now() in Jarvis's voice ("It's 4:05 in the afternoon, sir." / "It's Wednesday the 26th of August, sir."), spoken regardless of talk-back, so the 3B model never has to guess a time. Tests: tests/test_commander.py (Tier 1 clock section). |
| Spoken context rendering | Automatic: the Tier 2 prompt's background block | jarvis/context.py — ContextEngine.format_for_prompt(ctx, spoken=True) | live (verified offline) | Git line spelled out ("on branch main, 40 files changed and not yet committed, last commit: ...") so the small model no longer reads "40 changed files" as "up to date"; recently-modified file names collapse to a count and the commit hash is dropped for the spoken prompt (Tier 3 keeps names and hash). Tests: tests/test_context.py. |
| Barge-in (typed) | Type any command while Jarvis is talking — the current line is cut and the queue dropped before the new command is answered; also `JarvisApp.interrupt_speech()` for the UI's first-keystroke hook (see scratchpad ui_hooks_todo.md) | jarvis/app.py — dispatch_text(), interrupt_speech(); jarvis/tts.py — TTS.interrupt(), stop() | verified-offline (needs app restart) | `interrupt()` returns True only when something was actually cut (counter `tts.interrupts`); playback process is terminated within ~0.1 s (poll loop), XTTS synthesis stops at the next sentence boundary. Voice barge-in is impossible by design today: the hotword stream is paused while Jarvis speaks. Tests: tests/test_tts_voice_io.py (barge-in section). |
| "Quiet" at Tier 1 | "Jarvis, quiet / be quiet / hush / shut up / that's enough / never mind / stop talking / stop reading / stop", with or without the prefix (jarvis mode); typed or voice | jarvis/commander.py — quiet_kind(), _h_quiet; REGISTRY entry "quiet" and the _route_text pre-brain check | verified-offline (needs app restart) | Cuts speech and any read-aloud in progress, replies "Very good, sir." in text only (never spoken — he was just told to be quiet). "stop recording/listening" keep their legacy stop-recording meaning. Tests: tests/test_reader.py (commander section). |
| Honest first-audio mark + Fish play-while-rendering | Automatic. The turn ledger's `wait` (turns.jsonl, "run diagnostics" median) now ends when the first player is spawned; with the fish engine, audio is piped to `paplay` stdin as the API streams it. `jarvis.tts.FISH_STREAM_PLAYBACK = False` restores render-then-play for an A/B by ear | jarvis/tts.py — TTS._mark_audio(), _worker_loop (publish moved), _play()/_run_amp_feeder(); _fish_iter() (byte seam; _fish_stream wraps it), _AudioStream, _LiveEnvelope, TTS._play_stream(); jarvis/app.py — _turn_on_speaking unchanged | verified-offline (needs app restart; live `wait` numbers to be re-read after) | `SpeakingState(active=True)` used to be published BEFORE `_speak_sync` rendered anything, so every `wait` under-counted by the whole first-chunk render + player spin-up (live log 2026-08-29: "turn:" landed 13-90 ms after "speaking (fish)"). It is now sent once per burst at `Popen` (a burst that never plays still gets its rising edge at the end, so the turn closes and the follow-up mic opens as before); filler discrimination in app.py is untouched. Fish: bytes are teed into the temp file (speech cache still filled, replays offline), the reactor envelope is computed from the bytes in flight, a failure AFTER audio was heard loses only that chunk's tail (never re-spoken by the local fallback), a failure BEFORE any audio falls back to F5 as before, and once retired for credit later chunks go straight to F5 with no doomed round-trip. paplay missing or dying at once → the complete file goes down the usual chain. F5/XTTS/edge keep the file path (F5's sidecar cannot stream). Also new: `TTS.speak()` returns the utterance's done-event; `TTS.skip_current()` cancels the current utterance and keeps the queue. Tests: tests/test_tts_first_audio.py. |
| Say again | "say again / say that again / repeat that / come again / pardon / what was that / once more" | jarvis/commander.py — repeat_kind(), _h_repeat; jarvis/tts.py — TTS.last_text, repeat_last() | verified-offline (needs app restart) | Replays the last cleaned utterance verbatim (from the speech cache when available — instant, identical rendition); shows the text in the transcript without re-adding it to the conversation. "I haven't said anything yet, sir." when nothing has been said. |
| Speech cache + prewarm | Automatic: every synthesized phrase is stored under ~/.aiws_trainer/tts_cache/ keyed by engine + voice parameters + spoken text; canned lines (courtesies, reminder preamble, quiet/read-aloud excuses) are rendered at startup while idle | jarvis/speech_cache.py — SpeechCache; jarvis/tts.py — _cached/_store/_cache_key, prewarm(); jarvis/app.py — _canned_phrases() | verified-offline (needs app restart) | Bounded LRU (400 files / 150 MB, mtime touched on hit); XTTS chunks are cached per sentence so a repeated sentence inside a new reply also hits. Measured on the GB10: XTTS warm synthesis 1.5-1.8 s per sentence (RTF 0.48), edge 1.0 s warm / 2.6 s cold — a cache hit costs a file stat. Prewarm yields whenever real speech is queued and shares the XTTS model under `_synth_lock`. Cache is disabled per instance with `TTS(cache=False)`. Tests: tests/test_speech_cache.py, tests/test_tts_voice_io.py. |
| TTS pronunciation dictionary | Automatic before synthesis; add entries with "Jarvis, pronounce Peyrovi as pay-ROH-vee" (also "pronounce X like Y"), or edit ~/.aiws_trainer/tts_pronunciations.json (reloaded on change; an empty value silences a default) | jarvis/pronounce.py — DEFAULT_PRONUNCIATIONS, Pronunciations, apply(); jarvis/tts.py — _pronounce(); jarvis/commander.py — _h_pronounce | verified-offline (needs app restart) | Whole-token, case-sensitive unless the key is lower-case ("nvidia" matches any casing); ~60 shipped entries for the workshop's jargon (VSS → "V S S", GB10 → "G B ten", CUDA → "kooda", Ollama → "oh-llama", XTTS, ChromaDB, xdotool, aarch64 …) plus symbols (% & °C ~/). The transcript keeps the real spelling; only the voice changes. The confirmation "Noted, sir. X it is." is spoken with the new entry. Tests: tests/test_pronounce.py. |
| Typed-command history | Automatic for typed input; `app.history.prev()/next()` for Up/Down in the command bar (UI hook pending, see scratchpad ui_hooks_todo.md); `recent(n)`, `search(prefix)` | jarvis/history.py — TypedHistory; jarvis/app.py — dispatch_text() | verified-offline (needs app restart) | Persisted as JSON lines in ~/.aiws_trainer/jarvis_memory/typed_history.jsonl (last 500, consecutive duplicates collapse with a count, shell-style cursor that any new entry resets). Tests: tests/test_history.py. |
| Read aloud | "read the clipboard" / "read what I copied", "read this" / "read the selection" (X primary selection), "read file ~/notes.md", "read aloud: <text>" / "read <text> aloud"; then "continue reading" / "go on" / "next part" for the next part; "quiet" stops | jarvis/reader.py — ReadAloud, chunk_text(); jarvis/commander.py — read_kind(), continue_kind(), _h_read_aloud, _h_continue | verified-offline (needs app restart) | Text is chunked at sentence boundaries into ≤380-char pieces the TTS accepts whole (its 500-char cap never truncates) and queued FIFO; one part (2400 chars ≈ 3 min) per request, then "That's the first part, sir. Say 'continue reading' for the rest." — deliberately outside the two-sentence reply rule because a document is not a reply. Files: text types only, 512 KB cap, resolved against cwd / ~ / ~/Jarvis. Excuses ("The clipboard is empty, sir.", "Nothing is highlighted, sir.") are spoken; "read notes" still lists notes. Tests: tests/test_reader.py. |
| Read-aloud steering: skip / back / pause / go on | While something is being read (or paused): "skip" / "skip that" / "skip ahead" cuts the chunk being spoken and the next follows at once; "back" / "go back" / "previous" / "say that again" re-reads the previous chunk then carries on; "pause" / "hold on" / "hang on" / "wait" holds the rest ("Paused, sir."); "go on" / "carry on" / "resume" / "keep going" picks up from the cut chunk. With or without the wake word (barge-in keeps the wake word live). "quiet" still ends the reading | jarvis/reader.py — ReadAloud.active/paused, skip(), back(), pause(), resume(), _fold_queued(); jarvis/tts.py — TTS.skip_current(), speak() returns the done-event; jarvis/commander.py — read_control_kind(), _h_read_control, REGISTRY "read control" (also ASSISTANT_TIER1), the 4b hook in handle(); jarvis/app.py — interrupt_speech() | verified-offline (needs app restart; voice path unverified without a mic) | Context-gated, never phrase-gated: the same words are Spotify's transport (router → spotify_control) and, prefixed, desktop media keys / "go back" = previous window, so the reader claims them only while `reader.active` is literally True — a chunk of a part is in the air or the reading is paused. Waiting for "continue reading" between parts is idle: Spotify gets its words back. The hook sits BEFORE the desktop chains, the media-key substitution and the intent gate, each of which would otherwise eat a one-word "skip". The reader owns the cursor (chunks are plain strings; TTS.speak's done-event says which is in the air) and a 200-chunk history for "back"; "pause" cuts the current ~25 s chunk and restarts it on "go on" (no sub-chunk position exists). Skip/back/go-on are confirmed by the reading itself carrying on; only "Paused, sir." and the excuses ("Nothing to skip, sir.", "We're at the start, sir.", "That was all of it, sir.") are spoken. Tests: tests/test_reader.py (steering + commander gating sections). |
| Voice diagnostics + first-mic checklist | `~/vss_env/bin/python -m jarvis.voice_check` (offline), `--load` (models), `--mic` (live first-mic session), `--json`; docs/voice-first-mic-checklist.md | jarvis/voice_check.py — parse_sinks(), parse_devices(), model_assets(), run_offline_checks(), run_mic_session() | verified-offline | Checks the PipeWire sink (flags the dummy/auto_null sink this machine currently has — speech plays into silence until an HDMI/USB output exists), capture devices, every model asset on disk, imports, CUDA; `--mic` records 4 s, reports RMS, transcribes, scores the wake word and the voiceprint, calibrates. Redirects JARVIS_LOG_DIR so it never touches the live app. Tests: tests/test_voice_check.py; synthetic-audio model checks in tests/test_voice_models_live.py (JARVIS_LIVE_MODELS=1). |
| Audio-output health warning | Automatic at model load | jarvis/app.py — _load_models(); jarvis/voice_check.py — output_sink_state() | verified-offline (needs app restart) | Status(warn) "No audio output device — speech will be silent" when the only sink is auto_null/Dummy (the case on 26 Aug: card profile `off`, all HDMI profiles `available: no`). |
| External speak-queue IPC | From any process: `echo "Jarvis: build finished" >> /tmp/vss_voice/speak_queue.txt` (PATHS.SPEAK_QUEUE); in-process: `jarvis.speak_queue.say(text)` | jarvis/speak_queue.py — Watcher, say(), set_sink() | live | Watcher tails the file every 1 s, joins all new non-empty lines into ONE utterance, hands it to the TTS sink (talk-back gated by JarvisApp._say) and truncates the file; content written before the app started is skipped as stale backlog; external truncation is handled. Lines go through the same cleaning/pronunciation/cache as everything else. Tests: tests/test_tts_speak_queue.py, tests/test_tts_voice_io.py (protocol, temp file only). |
| Wake-word verifier shim (fix) | Automatic when ~/.aiws_trainer/hey_jarvis_verifier.pkl exists | jarvis/hotword.py — VerifierShim, install_verifier(), verifier_feature_count(); listen-loop failure guard | verified-offline with synthetic "Hey Jarvis" clips | openwakeword 0.4.0 re-applies the custom verifier once per loaded model with that model's frame count, so the hey_jarvis verifier (1536 features) was also fed the timer/weather windows (3264/2112) and raised — only on a real wake word, which the loop silently swallowed: with the verifier file present the hotword could never fire. Measured before/after on three synthetic voices: 8 predict errors and score 0.23 → 0 errors, 0.975-0.993 (negatives 0.003). A size-mismatched or unreadable verifier is now skipped with a warn Status instead of killing the listener; three consecutive predict failures drop the verifier. Note: the user's verifier accepts synthetic voices too — it was trained against noise negatives, so speaker identity still comes from the ECAPA voiceprint, not the verifier. Tests: tests/test_hotword_recorder.py. |
| Canvas (coursework) | "what's due this week?", "any new grades?", "any announcements?", "when's the biosensors midterm?" | jarvis/tools/canvas.py — canvas_due(days), canvas_grades(), canvas_announcements(days); `canvas.base_url` / `canvas.token` (secret) | verified-offline (fake urllib seam: pagination, 401, empty, DST-safe dates); needs the user's token | Read-only Canvas REST v1 with a 6 s wall budget (5 pages max), 10-min course cache, spoken-friendly local dates through the DST change, planner-off fallback to per-course buckets. Lines: "Nothing due this week, sir." / "Canvas rejected the token, sir." / "I can't reach Canvas, sir." Tests: tests/test_canvas.py. |
| Deadlines in the briefing + deadline heads-ups | Part of "good morning" / "give me the briefing" ("Due: Lab 3 report for BIOSENSORS today 11:59 pm…"); unprompted "Sir, this is your reminder. Lab 3 report for BIOSENSORS is due in 3 hours" | jarvis/tools/briefing.py — build_briefing() sections["due"] via canvas_due(days=2) on the pool; jarvis/ui/views.py briefing_rows() DUE rows; jarvis/deadlines.py — DeadlineHeadsUp (started/stopped in app.py beside MeetingHeadsUp, state PATHS.MEMORY_DIR/deadlines_state.json); `canvas.heads_up_hours` (3) | verified-offline (fakes: registry, fetch_due seam, stub timekeeper/calendar); dark until `canvas.token` is set | One planner call every 15 min for the next two days; a reminder is filed only once its due time is within lead + 45 min (work handed in earlier is never announced), keyed course|title|due so a restart cannot repeat it; inside the lead it says the real time left. Silent everywhere when the token is unset: no Due line, no note, no Canvas call; a CanvasError skips the tick at debug level. Tests: tests/test_deadlines.py, tests/test_briefing.py, tests/test_ui_assistant.py, tests/test_app_wiring.py. |
| Exam countdown + next-exam answers | "when's my next exam?", "when is my next quiz", "how long until the biosensors midterm?", "how many days until my final exam"; the briefing's "Exam: Midterm 1 for BIOSENSORS, in 6 days, Tuesday at 9:00 am"; the evening-before "Midterm 1 for BIOSENSORS is tomorrow at 9:00 am" at 7 pm | jarvis/tools/canvas.py — exam_kind(), exam_candidates(), next_exam(), countdown_words(), find_next_exam(cfg, calendar, query); jarvis/commander.py — "next exam" Tier-1 (_NEXT_EXAM_RX / _h_next_exam, in ASSISTANT_TIER1); briefing sections["exam"] / EXAM row; deadlines.py _file_exam_eves | verified-offline (fake fetch + stub calendar); the Canvas half needs the token, the calendar half works today | No model turn: Canvas items (30-day planner window) merged with the calendar cache (14 days), the iCloud "Canvas" mirror deduped by title+day. exam/midterm/final are one kind, quiz another ("next exam" never answers with a quiz); "final"/"midterm" must appear in the title; other words must appear in course or title. Without a token and no calendar hit the question falls through to the router so canvas_due's setup line explains what is missing; with Canvas read and nothing found: "Nothing that looks like an exam on the books, sir." / "No quiz on the books, sir." Tests: tests/test_canvas.py, tests/test_commander.py, tests/test_deadlines.py. |
| Your own documents (local RAG) | "what does the syllabus say about late work?", "according to my notes …", "reindex my documents" | jarvis/tools/docs.py — DocsIndex (chromadb PersistentClient, nomic-embed-text via Ollama /api/embed, pdftotext), ask_docs(question), docs_reindex(); `docs.paths` (default ~/Documents/Jarvis Docs) | verified-offline (fake embedder, real chromadb at tmp) + live smoke (cold reindex 7.3 s, query 2.3 s then 0.01 s) | Chunks ~800 chars, incremental by (path, mtime, size), one background pass kicked at first use and re-kicked when a pass failed or new files appeared; explicit reindex runs off the brain thread with a 5 s wait. Top-5 chunks as a fact sheet naming the file. Tests: tests/test_docs.py. |
| Screen Q&A (local vision) | "what's on my screen?", "what does this error say?", "read me this" | jarvis/tools/screen.py — screen_qa(question): PIL.ImageGrab (ImageMagick `import` fallback; never gnome-screenshot) → ≤1280 px JPEG → Ollama llama3.2-vision; `screen.model` | verified-offline (fake grab + fake Ollama); vision call not exercised live yet | Active window title goes in as context; the answer is spoken directly (speak=) because the 25 s vision call exceeds the 8 s tool-loop budget for a second model turn. No image on disk unless JARVIS_DEBUG_SCREEN=1. Tests: tests/test_screen.py. |
| Spark health + memory watchdog | "how's the Spark doing?", "system health"; automatic warnings | jarvis/tools/health.py — system_health(), Watchdog (services.health_watchdog, started by the app with services.speak) | verified-offline + live read-only smoke ("Memory 88 of 122 GB free; GPU 43 C …") | Unified memory is invisible to the OOM killer, so the watchdog speaks once per episode under warn_gb (16) / critical_gb (8) and when two processes each exceed hog_gb (20) — the two-trainers pattern behind the 28 Aug hard power-off — with hysteresis on both rules. It never runs nvidia-smi (a wedged one cannot be waited for); the tool does, via Popen + kill-without-wait. Tests: tests/test_health.py. |
| Conversation memory · follow-ups · barge-in · first-wake briefing · heads-ups · guests · passive enrolment · diagnostics · streamed replies | see docs/assistant-setup.md §19 | jarvis/context.py (conversation rendered, 10-min TTL), jarvis/app.py (_after_dispatch/_after_speech/_start_followup/_on_guest/_maybe_learn_voice/_briefing_due/diagnostics_text/_on_stream_sentence), jarvis/recorder.py (start(followup=)), jarvis/headsup.py, jarvis/brain.py (_stream_round), jarvis/tts.py (barge_in, playback_device) | verified-offline; live numbers pending | Knobs: followup_window, barge_in, stream_replies, playback_device, briefing.on_first_wake/after, calendar.heads_up_min. Tests: tests/test_assistant_features.py, tests/test_streaming_replies.py, tests/test_diagnostics.py. |
| Web lookups via Claude | "look up …", "search the web for …", "google …", "who won …", "what's the latest news about …", "price of …", "is … open right now", "when does … come out", "ask claude to look up …" | jarvis/router.py — _WEB_CUE_RX → RouteDecision("web"); jarvis/commander.py — _web_lookup(), WEB_LOOKUP_LINE / WEB_UNAVAILABLE_LINE; jarvis/brain.py — JarvisBrain.web_answer(), clean_web_answer(), WEB_PROMPT; `claude.web_model` in assistant.json (haiku) | verified-offline (scripted CLI) + live one-shots (~12 s, haiku and sonnet both correct on the Dutch GP) | The only path that hands what you said to a web search (the assistant's own reasoning stays on the local model; the coding sessions, weather, calendars, mail and Discord each talk to their own service), chosen over a search package in the shared venv. A cue routes the question to a one-shot `claude -p --model <web_model> --allowedTools WebSearch,WebFetch` on a worker thread; Jarvis says "Looking that up, sir." at once (recorded by the turn ledger as a filler, not the answer) and speaks the answer when it lands through the brain callback that closes the turn. The prompt asks for two spoken sentences, no links; clean_web_answer() strips a "Sources:" tail, markdown links and URLs anyway (sonnet adds them regardless). 60 s budget → "That lookup is taking longer than I'd like, sir."; a failed CLI → "I couldn't get a straight answer from the web on that, sir."; brain busy → the usual "Still on the last one". Tests: tests/test_web_route.py, tests/test_app_wiring.py. |
| Speech endpointing + turn ledger | Automatic on every voice turn; knobs `endpoint_vad` / `endpoint_silence` (0.8 s) in voice_settings.json; `grep "turn:" /tmp/vss_voice/jarvis.log`, `/tmp/vss_voice/turns.jsonl` | jarvis/endpoint.py — VoiceEndpointer (Silero VAD, 2 MB TorchScript at ~/.aiws_trainer/silero_vad/silero_vad.jit, plain torch, no new package); jarvis/recorder.py — _check_endpoint(); jarvis/turnclock.py — TurnLedger; jarvis/app.py — _turn_on_stop/_turn_on_transcribed/_turn_on_speaking | verified-offline (scripted VAD + the real model on a speech clip); live numbers pending | The recorder stops `endpoint_silence` after the VAD last heard speech instead of waiting out the 2.5 s energy timer (which stays as the fallback for a capture in which nobody speaks; the voice-ID stop still handles a room where someone ELSE talks). Only ever stops after ≥4 chunks (~130 ms) of speech and after `silence_grace`, so the tail of "Jarvis" plus a thinking pause cannot end the capture early. 3.6 ms per 32 ms chunk on the GB10 CPU, run from the 12 Hz poll thread on the frames since the last tick. Every voice turn logs one line — `turn: wake→mic 61ms · speech 1.9s · dead-air 0.8s · stt 0.37s · route 1.2s · wait 2.6s (stop=vad)` — where `wait` is what the user feels (last word → first audio) and `stop` says which detector ended the capture; rejected/ignored turns log `turn[rejected:speaker]`. Tests: tests/test_endpoint.py, tests/test_turnclock.py, tests/test_app_wiring.py (ledger wiring). |
| Speculative transcription during the endpoint silence | Automatic on every VAD-ended voice turn; `listening.speculative_stt` (true) in assistant.json switches it off; the ledger line carries `decode=speculative` when the pass was reused | jarvis/app.py — _partial_loop() (one thread for the preview and the pass), _maybe_speculate(), _take_speculation(), _decode_clip(); jarvis/recorder.py — snapshot_final() / _shape_final(); jarvis/events.py — Transcribed.speculative | verified-offline (stubbed recorder + endpointer + transcriber); live saving pending a mic round | Every turn paid the full whisper decode strictly AFTER the recorder stopped while the GPU idled through the 0.8 s endpoint silence (ledger: dead-air 0.80-0.96 s, stt 0.40-0.67 s). Once the VAD has heard 0.3 s of silence the partial thread decodes the clip exactly as _process_audio would — the same 60 s cap / 0.3 s minimum / noise gate (recorder.snapshot_final), the same speaker filter, the same beam-5 transcribe — and if the stop is the VAD's own and no more speech followed (endpointer.last_speech_seconds unchanged), _process_audio publishes that result instead of decoding again; a pass still in flight is WAITED on, never doubled. Anything else (an energy/manual/cap stop, more speech, a failed pass) is discarded and the clip is decoded as before; the greedy preview is suspended while the pass holds the model. Expected saving ~0.4-0.5 s of `wait` on a short question (decode overlap, not the whole stt figure). The stt figure stays honest: stop→publish, with the note saying why it is small. Tests: tests/test_speculative_stt.py. |
| "Did not catch that" policy (a cue instead of silence) | Automatic after a wake-word turn that produced nothing to answer; `listening.nudge` (true) / `listening.nudge_cooldown_s` (30) in assistant.json | jarvis/app.py — _nudge(), NUDGE_LINE ("Sir?"), _on_recording_stopped / _process_audio call sites, _wake_pending → _turn_from_wake; jarvis/recorder.py — followup property, RecordingStopped.followup, the "nudge" beep; jarvis/events.py | verified-offline | By cause: nothing heard ("No audio captured" / "Too short" / an empty transcript) gets a short spoken "Sir?" (prewarmed) and a follow-up window so the question can just be repeated; a clip the speaker gate dropped, or a second garbled clip in a row, gets a non-verbal 440 Hz earcon only (a rejected clip may be a guest — a spoken line would answer them — or the user under a strict threshold, and re-opening the mic on a garbled room loops). Wake-word turns only: follow-up windows that hear nothing stay quiet (the recorder stamps `followup` on RecordingStopped), the mic button shows its own status, and nothing fires while he is already talking. One cue per 30 s across all causes so a noisy room cannot make him chatter; talkback off → earcon. Every abandon still lands in turns.jsonl (`no_audio`, `empty`, `rejected:speaker`, `rejected:confidence`) for tuning. Tests: tests/test_nudge_policy.py. |
| Assistant config (`~/.config/jarvis/assistant.json`) | Automatic: `AssistantConfig.load()` at boot (env `JARVIS_ASSISTANT_CONFIG` overrides the path); edit the file by hand (reloaded on change) or through settings (`get_option`/`set_option`); every tool asks `cfg.is_configured(section)` and speaks `cfg.setup_line(section)` when a section is blank | jarvis/assistant_config.py — DEFAULTS (spec 10.1), AssistantConfig.load/get/set/update/save/reload_if_changed, is_configured/missing_sections/setup_line/setup_lines, redacted/scrub, home_location/allowed_dirs/projects_root/local_model/skill_phrases, is_allowed_path(path, base)/add_allowed_dir/remove_allowed_dir; `_Section` live attribute views (`cfg.alarms.snooze_min`, `cfg.claude.skill_phrases`) | verified-offline (file creation exercised live on a scratch path) | Created from DEFAULTS with placeholders, dir 700 / file 600 (a loose mode is tightened on load); atomic save via mkstemp+fsync+os.replace, 600 kept; corrupt JSON moved to `assistant.json.bad` and recreated; new default keys are merged into an old file and written back; `load()` never raises (unwritable dir -> in-memory config, `save()` returns False). Placeholders (`""`, `null`, `<paste…>`, `PASTE-…`, `your-…`, `changeme`, `xxxx`) never count as configured. Secrets (`icloud.app_password`, `gmail.app_password`, `discord.bot_token`) show as `•••` in `redacted()` / `repr()`; `scrub(text)` strips their values from any string before logging. `is_allowed_path` resolves symlinks and `..` (a symlink inside a project pointing out is denied; `Jarvis_sibling` is not `Jarvis`). Sections: home_location (lat+lon), google_ical (an https URL), icloud, gmail, discord (token+channel), claude (binary on PATH or `JARVIS_CLAUDE_BIN` + allowed_dirs). Setup: docs/assistant-setup.md. Tests: tests/test_assistant_config.py (57). |
| Start at login + never sleep | `~/vss_env/bin/python -m jarvis.autostart --install` / `--status` / `--uninstall` (`--keep-suspend` leaves gsettings alone); from the app: `autostart.enabled` in assistant.json, the settings toggle, or `jarvis.app --install-autostart` (wiring item) | jarvis/autostart.py — render_desktop(), install(exec_cmd, path, working_dir), uninstall(), is_installed(), suspend_setting(run), disable_gnome_suspend(run), status(), main() | verified-offline (install/validate/uninstall exercised live on a temp HOME; `--status` on the real HOME: not installed, ac-type already 'nothing') | Writes `~/.config/autostart/jarvis.desktop` (`Exec=/home/hunterp/vss_env/bin/python -m jarvis.app`, `Path=/home/hunterp/Jarvis`, `X-GNOME-Autostart-enabled=true`, `X-GNOME-Autostart-Delay=15`; passes `desktop-file-validate`); idempotent — identical content is not rewritten (mtime unchanged). `disable_gnome_suspend` reads `org.gnome.settings-daemon.plugins.power sleep-inactive-ac-type` and only calls `set … nothing` when it is not already; every gsettings call goes through the `_run` seam (tests record, never execute). The app's pid-file guard makes a login launch beside a manual one harmless. The real entry is NOT installed yet (`autostart.enabled` defaults to false; run `--install` or flip the toggle). Tests: tests/test_autostart.py (15). |
| Away alerts hub (desktop toast + Discord) | Automatic (wiring item): the app calls `services.alerts.alert(kind, title, text, request_id=None)` on ClaudeTaskState(done/failed), ApprovalRequested, AlarmFired, ReminderFired and spoken Claude milestones; kinds `milestone / done / blocked / question / alarm / reminder` | jarvis/channels/notify.py — Alerts (attach, alert, flush), notify_argv(), discord_text(), `_run` seam; jarvis/channels/__init__.py — cfg_get(), is_placeholder(), redact() | verified-offline (needs wiring + app restart; a real toast was delivered on DISPLAY=:1 through the real `_run` seam) | Each alert fans out to `notify-send -a Jarvis -u <critical for question/alarm, else normal> -t <0 for question, else 8000> -- "<title>" "<text>"` and, when a configured DiscordChannel is attached, a post `**<title>** — <text>` (questions end "Reply yes or no."). One daemon worker thread with a queue: `alert()` returns in microseconds (test: < 50 ms with a 0.3 s toast + 0.2 s post), every delivery is capped at 5 s (subprocess timeout, Discord post budget), failures are logged with context and never raised, one bad delivery never stalls the next. `AlertRecord`s (`recent`, last 50) carry toast_ok/discord_ok for a status view. Optional switches `alerts.desktop` / `alerts.discord` (default on) in assistant.json; `alerts.desktop=false` silences EVERY banner, including the three direct notify-send sites (reminders, agent monitor, screenshots) — speech, Discord and the transcript are unaffected (re-read within 5 s of a hand edit). `milestone` never toasts; `blocked` is normal urgency (critical never auto-expires on GNOME); identical text within 30 s is delivered once (49e7393). Tests: tests/test_channels.py (Alerts section, 10 tests). |
| Discord two-way channel | Set `discord.bot_token`, `discord.channel_id` (and `discord.user_id` = your Discord user id) in ~/.config/jarvis/assistant.json (docs/assistant-setup.md); then anything typed in that channel — or a DM to the bot from `user_id` — reaches Jarvis as a command ("yes"/"no" answer a pending approval, anything else is an ordinary Jarvis request), and his replies come back to the channel for 10 minutes after each exchange | jarvis/channels/discord.py — DiscordChannel (configured, start/stop, post, mark_active/is_active, status_text, redacted), UrllibTransport, chunk_text(), gateway_url(), message_wanted(), INTENTS | verified-offline against a scripted fake transport; live check token-free only: `GET /api/v10/gateway` 200 in 0.11 s and a real websocket HELLO from `gateway-prd-arm-us-east1-c` (heartbeat_interval 41250 ms) in 0.36 s through the real transport; a placeholder token is refused 401 and the channel stays a no-op | Gateway thread (asyncio inside a daemon thread, `websockets` 15): `GET /gateway/bot` -> `wss://…/?v=10&encoding=json`, HELLO -> jittered heartbeat with the last sequence (a missing ACK closes 4000 and reconnects), IDENTIFY intents GUILDS|GUILD_MESSAGES|MESSAGE_CONTENT|DIRECT_MESSAGES (37377), MESSAGE_CREATE filtered to the channel (or a DM from `user_id`), never the bot itself, another bot or a webhook — and, when `user_id` is set, only that user even inside the channel (a "yes" in a shared channel can approve a git push); RESUME with session id + seq on drops (1-60 s backoff, op 7 RECONNECT and op 9 INVALID_SESSION handled); after 3 failed connection attempts (or close 4014 = Message Content intent not enabled) it polls `GET /channels/<id>/messages?after=<last_id>&limit=20` every 5 s (cursor established with limit=1 so history is never replayed; failures back off to 60 s); close 4004 (bad token) gives up with one ERROR line. `post()` splits at 2000 chars on newline/space boundaries, honours 429 `retry_after` (body, then Retry-After header) inside a caller-set budget, stops on 401/403/404. The token is never logged: every log line and exception text passes `redact()`, `repr()`/`redacted()` show only `token=set`, the `websockets` logger is pinned to INFO so its DEBUG frame dumps (which carry IDENTIFY) cannot fire; tests assert no record contains it across gateway-lookup, connect, recv, on_message and post failures. `on_message(text, author_id)` runs on the gateway thread — the app hands it to its worker (hooks in scratchpad hooks_W8.md). Tests: tests/test_channels.py (52 offline + 1 live). |
| Spotify — search & play | "play Blinding Lights", "play One More Time by Daft Punk", "play some Daft Punk" / "songs by the Weeknd" (artist top tracks), "play the album After Hours", "play my gym mix" / "play Discover Weekly" (own playlists first), "play Blinding Lights on my phone" / "on HPCOMPUTER" (named Connect device) | jarvis/tools/spotify.py — SpotifyTool.play(), infer_kind(), split_device(), resolve_device(); registry tool `spotify_play(query, kind?, device?)` | verified-offline (needs W10 wiring + a linked account; not yet run against the real API) | spotipy 2.26 (pip, ~/vss_env), Authorization Code flow, redirect http://127.0.0.1:8888/callback, token ~/.config/jarvis/spotify_token.json (0600, auto-refresh, a 401 refreshes once and retries); keys in assistant.json `spotify{client_id, client_secret, default_device:"HPCOMPUTER"}`. Output is a Spotify Connect device — including the Spark itself, which advertises as "Spark" through `jarvis-spotify.service` (scripts/jarvis-spotify-device: librespot 0.8, pipe backend → pacat → PipeWire; install steps in docs/assistant-setup.md §11) — chosen as: named in the utterance > `default_device` > the active device > "Nothing's listening, sir — open Spotify on HPCOMPUTER or your phone." (transfer_playback(force_play=False) when the target is not active). Type inferred from the phrase (by/album/my/playlist words, Discover Weekly-style names), else one multi-type search with exact artist/album/playlist wins over the first track. Market: `spotify.market` — set a country code (US here): `from_token` needs the `user-read-private` scope, which the token does not carry, so every search 403s "Insufficient client scope"; a 400 or a scope-denied 403 on the market is retried once without it and the market is dropped for the rest of the session (5f16326, 1d18238). An artist with no top tracks and no "This Is" playlist: "I can't find anything of Daft Punk's to play, sir." Search items that Spotify returns as null (restricted playlists for post-Nov-2024 apps) are dropped. Every reply is a fixed persona line returned as ToolResult.speak (no model turn) naming what actually started: "Blinding Lights by The Weeknd, sir — on HPCOMPUTER." Free accounts: "I'm afraid that needs Spotify Premium, sir." Tests: tests/test_spotify.py (FakeSpotify, fixtures tests/fixtures/spotify_*.json, 154 pass, incl. a signature-conformance check that FakeSpotify matches the real spotipy.Spotify; live read-only check with JARVIS_LIVE=1). Setup: docs/assistant-setup.md "Spotify" (scratchpad spotify_setup_section.md until W1 appends it); wiring snippets in scratchpad hooks_SPOTIFY.md (`--spotify-login` flag, DEFAULTS/SECRET_KEYS, router cues). |
| Spotify — Liked Songs on shuffle | "play my liked songs", "shuffle my likes", "play my library" (also via spotify_play) | jarvis/tools/spotify.py — SpotifyTool.liked(); registry tool `spotify_liked(device?)` | verified-offline (both strategies against the fake; the live winner is unmeasured) | Pages /me/tracks 50 at a time (cap 500, local files skipped), shuffles client-side. Strategy `uris` (default, documented API): start_playback with the first 100 uris, then a daemon thread queues the next 20 with add_to_queue. Strategy `collection`: shuffle(True) + context `spotify:user:<id>:collection` — undocumented, reported to 403/404 for many third-party apps, so it is the fallback; each strategy falls back to the other on a non-fatal error (Premium / no-device / auth errors stop immediately). `spotify.liked_strategy`, `liked_chunk`, `liked_queue_ahead`, `liked_cap` tune it. Lines: "Your Liked Songs on shuffle, sir — 230 of them, on HPCOMPUTER." / "…, sir — on HPCOMPUTER." (collection) / "Your Liked Songs are empty, sir." |
| Spotify — transport, volume, now playing, like | "pause", "resume", "next" / "skip", "previous", "volume 40", "louder" / "quieter" / "mute", "shuffle on/off", "repeat this song" / "repeat off", "jump to 1:30", "what's playing", "like this song", "move it to HPCOMPUTER" / "play it on my phone" | jarvis/tools/spotify.py — SpotifyTool.control(), now_playing(), _like(); registry tools `spotify_control(action: pause|resume|next|previous|volume|shuffle|repeat|seek|like|transfer, value?)`, `spotify_now_playing()` | verified-offline | Transport targets the ACTIVE device first (named > active > default) so "pause" never drags playback off the phone; "resume" with nothing active wakes the default device. Values coerced loosely: "40"/"40%"/40/"up"/"down"/"max"; seek "1:30"/"90 seconds"/"2 minutes"; repeat one/all/off. Lines: "Paused, sir." "Resumed, sir." "Skipped, sir." "Back one, sir." "Volume 40, sir." "Shuffle on, sir." "Repeat on this track, sir." "Jumped to 1:30, sir." "Blinding Lights by The Weeknd, sir — on HPCOMPUTER." (paused: "…, sir, paused on HPCOMPUTER.") "Nothing's playing, sir." "Saved Blinding Lights to your Liked Songs, sir." / "… is already in your Liked Songs, sir." / a podcast episode: "That's a podcast, sir; I only save songs." "Moved to HPCOMPUTER, sir." (transfer uses force_play=True). Errors: NO_ACTIVE_DEVICE 404 -> the nothing's-listening line; 403 restriction -> "Spotify won't allow that just now, sir."; 429 -> "Spotify's rate-limiting me, sir; give it a moment."; network -> "I can't reach Spotify, sir."; anything else -> "Spotify isn't answering, sir." (ok=False, text carries the status for the log). |
| Spotify — queue & radio | "queue Save Your Tears next", "play something like this", "something like Daft Punk" | jarvis/tools/spotify.py — SpotifyTool.queue(), radio(); registry tools `spotify_queue(query)`, `spotify_radio(seed?)` | verified-offline | Queue: track search -> add_to_queue on the active device -> "One More Time by Daft Punk is up next, sir." Radio without /recommendations or related-artists (both closed to apps created after 27 Nov 2024): seed = the current track's artist (or the named artist/track), search "This Is <artist>" and "<artist> Radio" playlists, exact-name match with Spotify-owned first -> play that context ("This Is The Weeknd, sir — on HPCOMPUTER."); otherwise the artist's top tracks minus the current one, shuffled, and he says so: "No radio for The Weeknd, sir, so it's their top tracks on HPCOMPUTER." Nothing playing and no seed: "Nothing's playing to go on, sir; name an artist and I'll oblige." |
| Spotify — one-time link | `python -m jarvis.app --spotify-login` (hook pending in app.py) or `python -m jarvis.tools.spotify --login` / `--status` / `--no-browser` | jarvis/tools/spotify.py — SpotifyTool.login(), login_cli(), TokenCache | verified-offline (fake auth manager; the real browser round trip is untested) | spotipy starts a loopback HTTP server on 127.0.0.1:8888 and opens the default browser (Brave; `DISPLAY=:1` set if unset); the token is written 0600 in a 0700 dir, atomically. Prints and, when `jarvis.pid` exists, speaks "Spotify's linked, sir." through the speak queue. Unlinked: "Spotify isn't linked yet, sir; run me with --spotify-login once."; a failed refresh: "Spotify's link has lapsed, sir; …"; keys missing: cfg.setup_line("spotify"); spotipy missing: "The Spotify library isn't installed, sir; pip install spotipy into the venv." (the app boots regardless). Scopes: user-read-playback-state user-modify-playback-state user-read-currently-playing user-library-read user-library-modify playlist-read-private playlist-read-collaborative user-top-read. The six tool schemas add roughly 400 prompt tokens (1.6 KB of JSON) to the /api/chat tools list; if the W5 latency bar is missed, `spotify_liked` and `spotify_radio` are the first to fold into `spotify_play(kind="liked"/"radio")`. |
| Notes & to-dos | "note that the Bettany voice is the XTTS one", "add buy milk to my list", "what's on my list" / "read my notes", "mark the second one done" / "done with the dentist", "scratch buy milk off the list", "search my notes for voice" | jarvis/tools/notes.py — NotesStore (SQLite `notes` + `todos` under PATHS.MEMORY_DIR/notes.db), add/list/count/search/resolve/remove/complete, list_text/search_text, import_legacy(); registry tool `notes(action: add\|list\|remove\|search\|done, kind: note\|todo, text?, which?)` | verified-offline (exercised in-process: full CRUD round trip through the registry) | Every confirmation is returned as `ToolResult.speak`, so the spoken line needs no model turn: "Noted, sir." / "Added to your list, sir." / "Done, sir; two left." / "Struck off, sir; one left." / "Three to-dos, sir: buy milk, call the dentist, and fix the bike." `which` resolves `last`/`latest`, an ordinal or index ("the second one", "2", "#2", "number two") indexing the same chronological order `list_text` speaks, or a substring (exact match first, else most recent). Loose model values are coerced (action synonyms "remember"/"strike"/"tick", kind words "tasks"/"memos", kind inferred from the phrasing when omitted). `import_legacy` folds the old `note_*.txt` voice notes in once (meta marker row). Tests: tests/test_notes_mail.py. |
| Gmail summaries (unread, last 24 h; "my last email" reads read mail too) | "any mail?", "what's in my inbox", "read me my email" — needs `gmail.address` + `gmail.app_password` (app password, docs/assistant-setup.md) | jarvis/tools/mail.py — fetch_unread() (IMAP4_SSL, `SELECT INBOX` readonly + `BODY.PEEK`), make_snippet(), fact_sheet(), when_text(); registry tool `get_mail(limit?)` | verified-offline (fake IMAP with canned RFC 822 bytes; no credentials on this machine) | Read-only by construction — readonly SELECT and PEEK fetches, so nothing is ever marked read. `SEARCH UNSEEN SINCE <date>` (English month names regardless of locale), headers decoded through RFC 2047, snippet = first 200 chars of the text/plain part (text/html stripped) with quoted lines, "On … wrote:" and signature blocks dropped. The tool hands the model a fact sheet ("3 unread since yesterday:\n1) Jane Doe — Invoice 4471 due Friday (2:10 pm): …") with `max_sentences=4` and the local model speaks the summary; zero unread short-circuits with "Nothing new in the inbox, sir."; "what was my last email about?" is a Tier 1 command that forces `get_mail(unread_only=false, limit=1, since_hours=168)` because the model kept choosing unread_only=true on its own (fe233ff, 1d18238) and an IMAP failure with "I can't reach your mailbox, sir." Unconfigured speaks `cfg.setup_line("gmail")`. The password and message bodies are never logged (the address is masked to `h…@gmail.com`). Tests: tests/test_notes_mail.py. |
| Morning briefing (weather + calendar + tech/AI news) | "brief me", "morning briefing", "what's the news" — always answered when asked; `briefing.enabled` (off by default) only decides whether a plain "good morning" turns into a briefing | jarvis/tools/briefing.py — build_briefing(), fetch_news(), hn_top(), parse_feed()/feed_first_item() (stdlib xml.etree, Atom + RSS 2.0 + RDF), fetch_sports(), fetch_stocks()/parse_stooq_csv()/parse_yahoo_json(); registry tool `get_briefing()` | live (exercised in-process against the real feeds: HN + The Verge + Ars, plus live Open-Meteo weather) | `briefing.enabled` (off by default, spec 10.1) governs only the "good morning" trigger in commander._h_briefing; an explicit request runs regardless — the tool's own gate used to refuse those too (89b6833). Weather and calendar come through the registry (`get_weather{when:today}`, `get_calendar{range:today}`), news is Tech & AI only — top Hacker News stories by score over the first 30 top-story ids plus the first non-culture item of each configured feed (The Verge, Ars Technica), round-robin interleaved, deduped by normalised title and cut to 3; sports/stocks appear only when `briefing.sports_feeds` / `briefing.stock_symbols` are set (stooq CSV, Yahoo chart JSON as fallback). Every source is fetched in parallel through one bounded pool and one `_fetch` seam; any source failing degrades to the sections that worked, and a partial result is not cached (news cache 15 min at PATHS.CACHE_DIR/news.json). The tool returns the fact sheet with `max_sentences=6` and `card=sections`; the brain emits `("BRIEFING", json)` then `("SPEAK", …)` and the app publishes `BriefingReady`. Real run 2026-08-26: "Weather: Today: high 100, low 76, mostly sunny, 4% chance of rain, sunset 7:55 pm. News: 1) AWS Acquires DuckLabs (Hacker News) 2) Nvidia is about to be a hundred-billion-dollar-a-quarter company (The Verge) 3) AI agents meant to replace Meta workers made "large-scale, disruptive actions" (Ars Technica)". Tests: tests/test_briefing.py (live feeds with JARVIS_LIVE=1). |
| Good-night preview of tomorrow (+ "Shall I wake you at seven?") | "good night" / "goodnight" (with `briefing.enabled` on — the same flag that makes "good morning" a briefing); always on request: "what does tomorrow look like", "how's tomorrow looking", "preview tomorrow", "tomorrow's briefing", "the evening preview" | jarvis/tools/briefing.py — build_preview(), wake_offer(); `get_briefing(when="tomorrow")`; jarvis/commander.py — _goodnight_preview() (hung off the courtesy handler, which answers "good night" first), _h_preview(), _PREVIEW_RX, Commander._try_alarm_offer(); jarvis/app.py — _on_brain_tags() speaks the card's `offer`; jarvis/ui/views.py — briefing_rows() CANVAS / TO-DOS / ALARM / OFFER rows | verified-offline (tests/test_briefing_views.py; live voice path unverified — no mic on the Spark) | One breath at bedtime: the night line ("Good night, sir. Tomorrow, briefly.") speaks at once, then the tool lands — tomorrow's weather (`get_weather{when:tomorrow}`), the first event and where (the parked CalendarSource's events when configured, else `get_calendar{range:tomorrow}`), Canvas due within a day (`canvas_due{days:1}`, silent while `canvas.token` is empty), open to-dos (`notes.list("todo")`) and the alarm that is set (`timekeeper.list("alarm")`). The fact sheet carries none of the offer: when the first timed event starts by `briefing.early_before` (09:00) and no alarm rings tomorrow before it, the tool parks `{due, time, label, line, made_at}` on `services.alarm_offer` and puts the line in the card; the app says it verbatim after the spoken preview and opens the follow-up window; the commander reads the yes/no BEFORE the intent gate (step 3d, `parse_yes_no`) and files `timekeeper.add_alarm(due, "wake up", "once")` — "Alarm at 7:00 am, sir." Anything not a clear yes/no drops the offer, as does an offer older than `OFFER_TTL_S` (180 s): a stray "yes" the next morning never sets an alarm. Wake time is `wake_lead_min` (60) before the event on the quarter hour; `briefing.wake_offer` false or `sections.alarms` off withholds it. No news at bedtime. `max_sentences=5` (2 when verbosity is brief); `PREVIEW_NOTHING_LINE` when every source is dark. Not built: a timed 21:30 delivery (needs an evening clock check like `_briefing_due`; the timekeeper can only speak text). |
| Week forecast ("how's my week looking?") | "how's my week looking", "how is my week looking", "how does my week look", "what's the week ahead", "weekly forecast / briefing / overview / workload", "how busy is my week", "brief me on my week" — "what does my week look like" / "what's on this week" stay calendar questions | jarvis/tools/briefing.py — build_week(), _canvas_items(), _week_text_items(); `get_briefing(when="week")`; jarvis/commander.py — _h_week(), _WEEK_RX; jarvis/ui/views.py — briefing_rows() WEEK summary row + one row per day | verified-offline (tests/test_briefing_views.py) | Seven days from today, bucketed day by day: calendar events (structured from the parked source, else parsed from `get_calendar{range:week}`'s "This week: today …; Tuesday …" line), Canvas items due within the week (`canvas_due{days:7}`, each sheet line "N) COURSE - title, Tue 11:59 pm" mapped to its day), reminders from the timekeeper, plus the open to-dos as their own section. The sheet leads with the count and a summary the model turns into "Heavy Tuesday, sir": "Heaviest: Tuesday (5); Clear: tomorrow, Wednesday, Saturday." (heavy = 3+ items, or the busiest day when it has 2; "A light week" otherwise; "Nothing on the week at all"). No news, sports or stocks. Card = `{summary, days:[{label, date, items}], todos, unavailable}`; `max_sentences=8` (4 brief); `WEEK_NOTHING_LINE` when nothing came through. The tool's ToolSpec grew one optional `when` (today/tomorrow/week, `coerce_when` takes "tonight"/"this week") rather than two new tools, keeping MAX_TOOLS. Not built: a Sunday-evening timed delivery. |
| Briefing preferences by voice (sections + verbosity) | "no news in the morning", "I don't want the weather in my morning briefing", "skip the sports in my briefing", "don't read the stocks in the daily briefing", "leave out canvas in the evening preview", "drop the reminders from my weekly forecast"; back on: "put the news back in the briefing", "include the to-dos in my briefing again"; "be briefer" / "shorter briefings" / "keep the briefings short"; "the full briefing" / "more detail" | jarvis/assistant_config.py — DEFAULTS["briefing"]["sections"] (weather, calendar, news, sports, stocks, canvas, todos, alarms, reminders: all true), ["verbosity"] ("normal"), wake_offer / early_before / wake_lead_min; jarvis/tools/briefing.py — section_on(), verbosity_cap(), VIEW_SENTENCES; jarvis/commander.py — _PREF_SECTION_RX / _h_pref_section(), _PREF_VERBOSITY_RX / _h_verbosity(), _persist_preference() (Tier-1 with and without the "jarvis" prefix) | verified-offline (tests/test_briefing_views.py) | The explicit half of backlog 33 only. A switched-off section is neither fetched nor mentioned (no "News: unavailable" for the model to apologise about): `build_briefing` skips the news pool / weather / calendar calls, `build_preview` and `build_week` skip their sections; a missing name counts as on, so a hand-typed typo cannot blank the card. Every voice-set preference is written to BOTH stores: `AssistantConfig.set("briefing.sections.<name>", bool)` — the file the briefing actually reads — and `memory.set_preference(key, value)` (jarvis/memory.py, previously written by nothing) as the record of what he asked. Verbosity "brief" halves each view's allowance via `ToolResult.max_sentences` (6→3 today, 5→2 tomorrow, 8→4 week); ordinary replies are already capped at MAX_SPOKEN_SENTENCES=2, so the briefings are the only place the knob can bite. Needs the assistant config service; without it the phrase falls through. Implicit learning (artists, volume, barge-ins) deliberately not built. |
| Study / focus sessions (pomodoro) | "study session biosensors", "start a fifty-minute focus session", "pomodoro", "25 minute study session for signals with a ten minute break"; mid-session "how long left" / "how much time is left in the block", "end the session" / "I'm done studying" | jarvis/focus.py — FocusSession (start/time_left/end/reconcile, _on_reminder), jarvis/tools/timekeeper.py — SILENT_PREFIX, add_silent_timer(), get(), is_silent/display_label; jarvis/commander.py — _FOCUS_START_RX/_FOCUS_LEFT_RX/_FOCUS_END_RX, _h_focus_start/_left/_end (REGISTRY + ASSISTANT_TIER1); app.py — _make_focus, services.focus, start_assistant → focus.reconcile() | verified-offline (real Timekeeper on a fake clock through the whole cycle, the real app typed end to end, restart with a late and a missed block; Spotify is a fake) | A block (`focus.block_min`, 25; "fifty-minute" sets 50) then a break (`focus.break_min`, 5; "with a ten minute break" sets 10), repeating until "end the session" or `focus.max_blocks` (4; 0 = until told). Every block, halfway mark and break is a SILENT timekeeper item (label "focus: block 1" …) in the sqlite store, so the session survives a restart; `Timekeeper._fire` skips its own speech/toast for those and publishes `ReminderFired(item_id, kind, silent=True, late)` — the session speaks "Halfway, sir." (blocks of 10 min or more), "Time for a break, sir; that's block 1 done. Five minutes off.", "Break's over, sir. Block 2.", and at the end "Session over, sir: three blocks of 25 minutes." State: `focus_session.json` under PATHS.MEMORY_DIR. Boot: `reconcile()` after `Timekeeper.start()` — a block that came due < 1 h ago fires late into the break (a late halfway stays quiet), a session missed by longer lapses with the count ("Your study session lapsed while I was down, sir; two blocks done."). Music (`focus.music`): "pause" pauses Spotify for the block and resumes it for the break, "playlist" plays `focus.playlist` for the block and pauses it for the break, "off" leaves it alone; calls run off-thread via services.spotify and every SpotifyError is swallowed (no device = no music, never an apology), setup/auth errors switch music off for the session. "how long left" with no session reads the timer list; "end the session" with no session falls through the ladder (it may be a Claude session). "focus session on …" is exempted from the "focus <window>" targeting phrase; the classifier learnt "session"/"pomodoro"/"study"/"focus". No DND/quiet-hours (not in the codebase). Tests: tests/test_focus.py (38), tests/test_app_wiring.py. |
| Lecture notes by voice (per course, searchable) | "notes for biosensors" / "take notes for signals" / "lecture notes on physiology" opens the capture; every accepted utterance is then filed until "end notes" / "stop notes" / "close my lecture notes" | jarvis/lecture.py — LectureNotes (add/close), slugify, note_path, resolve_course/_match_course; jarvis/commander.py — Commander.lecture_course, _handle_lecture (checked right after dictation), _LECTURE_RX/_LECTURE_END_RX, _h_lecture_start (REGISTRY + ASSISTANT_TIER1); jarvis/tools/docs.py — DocsIndex.kick(), services.docs_index; app.py — _after_dispatch/_dispatch re-open the mic on a "Noting:" result, _capture_window(); recorder.py — start(followup, window), clamp_window, MAX_FOLLOWUP_WINDOW_S | verified-offline (real commander + NotesStore + a real chromadb DocsIndex over a fake embedder; Canvas monkeypatched) | Lines go to `<docs.paths[0]>/notes/<course-slug>-<YYYY-MM-DD>.md` as `- HH:MM  text` under a `# Course — date` header (folder created, same-day reopen appends) and to the NotesStore tagged `course:<slug>`; "end notes" says "Notes closed, sir: 12 lines for BIOSENSORS." and kicks a background reindex (`DocsIndex.kick()`, bypassing the once-per-process latch) so "what did I write about impedance?" works through ask_docs. The course name is tidied against the Canvas roster when `canvas.token` is set ("biosensors" → "BIOSENSORS"), else used as said; an unwritable folder is refused at "notes for X". The mode is strict like dictation: nothing else routes until "end notes" (speaker verification still gates the mic). NOT a hands-free mic: after each noted line the mic re-opens without the wake word for `lecture.window_s` (20 s, capped at 30 = half the 60 s hard cap) and closes quietly if nothing is said — then it is the wake word again; the mic never stays open across a whole lecture. Notes are not spoken back and are kept out of the conversation memory. Tests: tests/test_lecture_notes.py (35), tests/test_app_wiring.py. |
| Reminders, timers & alarms (timekeeper) | "remind me to call the dentist in 20 minutes", "set a 15 minute timer", "wake me at 7am", "set an alarm for every weekday at 6:30", "what's on my schedule" / "any timers running", "cancel that" / "cancel all alarms"; while an alarm rings: "stop" / "snooze 10" or the UI modal | jarvis/tools/timekeeper.py — Timekeeper (start/stop, add_reminder/add_timer/add_alarm, list/list_text, cancel, stop_ringing/snooze/ringing, tick/catch_up, import_legacy), parse_when/parse_when_full/split_when/parse_duration, describe_due/duration_words/sentence_case, next_repeat, write_alarm_wav, make_tools -> set_reminder / set_timer / set_alarm / manage_schedule | live (exercised in-process: real scheduler thread fired a reminder, a timer and an alarm; snooze, dismiss, weekday reschedule, catch-up and the 4 tools all checked; paplay/notify-send through a recording seam, no audio played) | One SQLite table `items(id, kind, label, due, created, repeat, state, snooze_until, fired_at, note)` under `PATHS.TIMEKEEPER_DB`, so everything survives a restart; a daemon thread ticks every second (0.3-1 s while ringing). Reminders/timers speak once ("Sir, this is your reminder. Call the dentist." / "Sir, your 5-minute timer is up."), toast, publish `ReminderFired`, go `done`. Alarms speak "Sir, it's 7:00 am. Time to get up.", publish `AlarmFired` (UI modal) and loop `paplay --volume=<alarms.volume>` with 1 s gaps; after 30 s `alarms.escalate` switches to full volume and 0.3 s gaps; after `alarms.max_ring_s` (300) they stop, go `missed` and say so. Sound: `alarms.sound`, else the freedesktop alarm oga, else a generated 880/660 Hz WAV written once to the cache dir. `snooze(minutes)` (default `alarms.snooze_min` 10) and `stop_ringing` publish `AlarmStopped`; `daily`/`weekdays` alarms re-arm on stop (weekends skipped). `catch_up()` at boot: items < 1 h late fire now with "While I was down, sir: …", later ones are announced missed once in a single sentence, and a row left `ringing` by a crash is recovered. `parse_when` (pure, takes its `now`; `prefer="morning"` for alarms) handles "in 20 minutes/an hour and a half/half an hour/3 days", "at 3 pm", "at 6:30", "half past 6", "quarter to 8", "noon/midnight", "tonight/this evening", "tomorrow (morning/afternoon/evening/night)", "tomorrow at 8", "on friday at 9", "next monday", "day after tomorrow", ISO dates, and "every day/weekday at 7" (first occurrence + repeat), with bare-hour disambiguation to the next occurrence. `workflows.Reminders`' `reminders.json` is imported once at boot (the file is renamed `.migrated`). Tests: tests/test_timekeeper.py (133). |
| Location and local time | "where am I", "what time is it in Tokyo", "what's the date there" | jarvis/tools/location.py — resolve_home(), lookup_ip(), geocode(), now_in(), clock_words()/date_words(); registry tools `get_location()`, `get_time(location)` | live (exercised in-process 2026-08-26) | Home comes from `home_location` in ~/.config/jarvis/assistant.json (lat/lon, or a city name that is geocoded), else an IP lookup (ipapi.co, ip-api.com fallback) cached 24 h at PATHS.CACHE_DIR/location.json; `location_lookup: false` disables the network and the tool speaks the setup line. Named cities go through the Open-Meteo geocoder, cached per name in memory and on disk. No lookup ever exceeds 4 s. Real run: "You're in College Station, Texas, sir (from your network address)." / "It's 9:42 am in Tokyo, sir. It's Thursday the 27th of August there." Tests: tests/test_weather_location.py (live geocode/forecast/IP with JARVIS_LIVE=1). |
| Weather | "what's the weather", "what's it like outside", "weather tomorrow", "what's the week look like", "weather in London" | jarvis/tools/weather.py — fetch_forecast(), format_weather(), describe_code() (WMO table), coerce_when(); registry tool `get_weather(when, location)` | live (exercised in-process against Open-Meteo 2026-08-26) | Open-Meteo, no API key, US units (°F, mph, inch), the location's own timezone, 7 days; one forecast call cached 10 min per rounded (lat, lon). Four shapes — now / today / tomorrow / week — rendered as compact plain text the local model speaks in at most two sentences; clear skies by day read as "sunny", snow codes say "chance of snow". Failures return one clause, "weather service unreachable". Real run: "95°F and sunny, feels like 97, wind 6 mph; high 100, low 76, 4% chance of rain." / "In London, England: 69°F and overcast, feels like 71, wind 6 mph; high 78, low 62, 100% chance of rain." Tests: tests/test_weather_location.py. |
| Calendars (Google iCal + iCloud CalDAV) | "what's on today", "what's on tomorrow", "what does my week look like", "what's next" | jarvis/tools/calendar.py — CalendarSource (refresh thread, conditional GET, disk cache), parse_ics() (icalendar + recurring_ical_events), _fetch_icloud() (caldav), format_events(), describe_due(), as_of_words(); registry tool `get_calendar(range)` | verified-offline (no real calendar configured; fixtures + a live public Google ICS with JARVIS_LIVE=1) | Read-only, zero OAuth: Google "secret address in iCal format" URLs (`google_ical_urls`) fetched with If-None-Match/If-Modified-Since every 10 min, and iCloud via CalDAV with an app-specific password (`icloud.apple_id` / `icloud.app_password`). Recurrences are expanded, all-day events are listed first ("all day: Mum's birthday"), everything is converted to the system timezone, cancelled events dropped, duplicates across sources collapsed. `get_calendar` never fetches on the model's thread — it answers from PATHS.CACHE_DIR/calendar_cache.json and, when that is older than 10 min, triggers a background refresh and appends "That's as of 8:50 am."; a failing source keeps its previous events. Unconfigured speaks the setup line verbatim. Secret URLs and the app password never reach the cache file or the log. Tests: tests/test_calendar_tool.py with tests/fixtures/calendar_sample.ics (weekly recurrence, all-day, multi-day all-day, UTC + Europe/London edges) and tests/fixtures/icloud_sample.ics. |
| Claude coding sessions (per-project tmux running the real Claude Code TUI) | "Jarvis, have Claude fix the router" / "work on the haymaker digest" / "start a new project called weather bot" / "use fable" / "fast mode on" / "stop that" / "what's Claude doing?" / with the pop-out open, "in the terminal, run the tests" and "tell it to check the parser" (router rule 1b) / the terminal button (router hands these to `services.claude`) | jarvis/claude_session.py — ClaudeSessionManager (submit/cancel/work_on/new_project/resume/set_model/set_fast_mode/open_terminal/**terminal_open**/**open_projects**/ensure_project_settings/status_text), build_command(), _ensure_tmux()/_ensure_claude()/_await_ready()/_send_prompt(), _tail_transcript()/_drain(), **encode_project_dir()/transcript_dir()/find_transcript()**, **entry_turn_text()/parse_transcript_entry()** → parse_stream_event(), **pane_state()/pane_working()/sanitize_keys()**, discover_sessions()/read_session()/pick_session()/session_summary(), estimate_size(), `_run` seam. describe_event() and the `render` subcommand are still present and still tested but the pane no longer uses them | live (interactive pane driven against Claude Code 2.1.247 and tmux 3.4 on 2026-08-27; 69 tests in tests/test_claude_session.py) | One tmux session `jarvis-<slug>` per project, one task at a time per project (others queue; a second project only with `parallel=True`). The pane runs Claude **interactively** — `clear; cd <path> && claude --model M [--session-id <uuid> or --resume <id>] [--effort <lvl>] [--settings '{"fastMode":true}'] --permission-mode acceptEdits --mcp-config <mcp_jarvis.json> --permission-prompt-tool mcp__jarvis__approve --append-system-prompt-file <system_suffix.txt> '<prompt>'` — no `-p`, no `--output-format`, no pipe, so the pop-out holds the genuine Claude Code interface (thinking indicator, tool rendering, diffs, spinners) instead of a flat log. A session Jarvis starts carries the prompt in **argv**: keys typed at a TUI that is still claiming the tty are dropped, and argv cannot be. A session already warm is typed into with `send-keys -l` and `_send_prompt()` confirms the first 24 characters are visibly in the input box before pressing Enter, retrying up to 3× with `C-u` between tries; `sanitize_keys()` flattens newlines, tabs and control characters to one line first (a newline would submit, a tab would complete). Geometry is **manual**: a detached session ignores `new-session -x/-y` (`window-size latest` falls back to 80x24 and the TUI wraps into soup), so `_ensure_tmux()` also sets `window-size manual` + `resize-window -x 200 -y 50`, and only while no client is attached; `open_terminal()` hands the size back with `window-size latest` before a viewer attaches. Startup dialogs — which a read-only pane could never answer — are read by `pane_state()` (trust / settings / menu / ready / starting): workspace trust is accepted only for a folder already cleared (`unsafe_dir()` clean and `project_allowed()`), otherwise Esc and the task fails; the Settings Warning is answered "Continue"; any other menu is left alone and the task fails loudly. **Progress comes from the transcript, not stdout.** `encode_project_dir()` maps the cwd to Claude Code's `~/.claude/projects/<dir>` name (every non-alphanumeric character becomes `-`), `find_transcript()` takes the file named by the session id, else the newest `*.jsonl` touched since the prompt went in, and `_tail_transcript()` follows it from the byte offset taken **before** the prompt, so a resumed session never replays its history. `parse_transcript_entry()` drops sidechains (subagents) and Claude Code's own bookkeeping records and hands the rest to the same `parse_stream_event()` print mode used — the transcript's `message` shape is identical — so every milestone rule is unchanged code: first Edit/Write per file coalesced, "Running the tests" / "Tests passed, sir" / "N tests failed, sir", plan chosen, three errors in a row, rate-limited to one spoken line per 20 s except start/done/blocked/question. Completion is `entry_turn_text()` — the assistant message whose `stop_reason` is `end_turn`/`stop_sequence`, with nothing after it for `settle_s` — never a fixed sleep; its text becomes `Task.result_text` for the spoken two sentences. Guards: the `claude.max_task_s` timeout (7200 s), `idle_s` (300 s) of transcript silence with a pane not showing "esc to interrupt" ends with "Claude stopped without a result, sir.", and a `tmux list-panes` liveness check every 5 s catches a vanished process. Only the prompt is written under `task_dir/<slug>/` now — no `.jsonl`, `.rc` or `.stderr` — and the transcript path is recorded in `Task.paths`. Model: `--model opus` by default, `fable` on a large size estimate or "use fable", sticky per session in `claude_projects.json`. A brand-new pane gets a `--session-id` we generate, so the transcript path is known before the first byte lands; a project worked in before gets `--resume <id>` (never `--continue`, which would hijack Hunter's own terminal session), and a resume failure retries once without it. **Cancel is `C-c` to the pane and nothing else** — the pane's child IS the session Hunter is watching, so the old SIGINT-the-pane-children step is gone and the interactive Claude survives with its scrollback. `terminal_open(project=None) -> bool` (`tmux list-clients -t jarvis-<slug>`, TTL-cached 1 s so the router may ask freely) and `open_projects() -> list[str]` (newest project first) tell the router and the UI when someone is watching. `open_terminal()` attaches **read-only** — `gnome-terminal --title "Jarvis · <slug>" --geometry=200x50 -- tmux attach -r -t jarvis-<slug>` — so keystrokes in that window cannot drive or derail the session and Jarvis stays the only input path; a second click raises the existing window via `xdotool search --name`. Known gap: a question the TUI asks **inside** the pane (AskUserQuestion, plan approval) has no answer path from a read-only terminal, so the task runs out on the idle guard; ordinary permission asks are fine because they leave the pane through `--permission-prompt-tool`. `ALLOW_RULES` names the server (`mcp__jarvis__*`): a bare `mcp__*` is an invalid allow rule in CLI 2.1.247 and interactively stops the session on a modal Settings Warning, so `STALE_ALLOW_RULES` prunes it out of any `settings.local.json` an older Jarvis wrote. Session discovery walks `~/.claude/projects/*/*.jsonl` (any session on this machine, including Hunter's own), skips meta lines, tool-result turns, Jarvis's own Tier-3 sessions and dead cwds, and ranks by name overlap + date words ("yesterday") + recency + keyword hit; a tie inside 0.15 asks ONE persona question naming both. New projects scaffold under `~/projects` (git init, CLAUDE.md, README) and are auto-allowed. Tests: tests/test_claude_session.py, fixture tests/fixtures/claude_stream_sample.jsonl. |
| "What's Claude doing?" (spoken session digest) | "what's Claude doing?" / "how's Claude getting on" / "is Claude still working" / "is Claude done yet" / "any word from Claude" / "Claude status" / "what's Claude up to" | jarvis/router.py — `_STATUS_RX` (first check in `Router._action`, action `status_text`); jarvis/claude_session.py — `ClaudeSessionManager.status_text()`, `own_sessions()`, `_capture_pane_named()`, `Task.last_spoken` / `Task.question`, `IDLE_LINE`; commander `_dispatch_action` speaks the returned string | verified-offline (tests: tests/test_router.py, tests/test_claude_session.py, tests/test_commander.py) | One line built from live state: project, elapsed, files touched so far, the last milestone Jarvis actually spoke (recorded on the Task where the milestone limiter decides it, so no app plumbing), an in-pane AskUserQuestion Claude is sitting on (`Task.question`, set by `_parse_tool_use`, cleared by its tool_result — distinct from state "waiting", which is an MCP permission prompt), how many tasks are queued, and the user's OWN `cc-<dir>` tmux sessions (`~/.bashrc`'s claude wrapper) with mid-turn/idle from `pane_working()`. The lock is held only to copy fields; the tmux probe runs unlocked. The idle half (`IDLE_LINE`) is prewarmed; the running line is dynamic. A bare "status" / "status report" is NOT this: those stay with the persona's git-line family and the diagnostics command. Previously the phrase was documented but `status_text()` had no caller, so it fell through to the local model. |
| Clipboard-to-Claude | "have Claude fix what I copied" / "ask Claude about the clipboard" / "send what I copied to Claude and fix the error" / "give Claude the clipboard" / "have Claude look at the selection and explain it" / "tell Claude to fix what I highlighted" (X primary selection: "the selection", "the highlighted text", "what I highlighted") | jarvis/commander.py — `_CLIP_TO_CLAUDE_RX`, `_clip_instruction()`, `_write_clip()`, `_h_clip_to_claude` (REGISTRY entry "clip to claude", also in ASSISTANT_TIER1), `CLIP_EMPTY_LINE` / `SELECTION_EMPTY_LINE`; jarvis/reader.py `_xclip` | verified-offline (tests: tests/test_commander.py `clip`) | The clip is written 0600 to `<active project>/.jarvis/clips/<timestamp>.txt` (dir 0700, `.jarvis/.gitignore` = `*` so the repo stays clean) and the prompt names the FILE — the clip is never inlined: `sanitize_keys` collapses a prompt to one line for `send-keys -l`, and the prompt is echoed to `task_dir/<id>.prompt`, the bus and Discord, so a pasted secret would leak. The project-local spot (not `task_dir`) keeps the interactive Claude inside `claude.allowed_dirs`, so no read prompt. The spoken words become the instruction with the clip phrase replaced by "the text in <path>" ("fix the text in …", casing kept from the raw text); a lone "about" is no instruction ("Have a look at …"). Then the ordinary Claude dispatch: model sizing, refusal lines spoken as-is, the local-model ack. Ordering trap: registered BEFORE the bare `clipboard` substring command, or "have Claude fix the clipboard" is read aloud; and in ASSISTANT_TIER1 because the hotword consumes the wake word, so spoken text never reaches the prefixed registry. Empty clip → "The clipboard is empty, sir." / "Nothing is highlighted, sir." (prewarmed); no active project → NO_PROJECT_LINE. A bare "this" is deliberately not a clip phrase ("have Claude fix this" stays a plain task); "the selection logic" and similar code nouns are excluded by lookahead. Active project only — no "in the X project" modifier. |
| Log triage — Jarvis reads his own log | "anything wrong in your log?" / "check your log" / "any errors in your log today" / "what's in your log" / "log triage"; "why was that slow?" / "why did that take so long" / "what took so long" / "where did the time go" / "break down that turn" | jarvis/logtriage.py — `parse_records()`, `normalise()`, `cluster_warnings()`, `read_tail()`, `read_turns()`, `last_turn()`, `turn_outliers()`, `triage_text()`, `slow_text()`, `NOISE`, `TAIL_LINES`; jarvis/app.py `log_triage_text()` / `slow_turn_text()` (services `log_triage`, `slow_turn`); jarvis/commander.py `_LOGTRIAGE_RX` / `_SLOW_RX`, `_h_log_triage` / `_h_slow_turn` (REGISTRY + ASSISTANT_TIER1); jarvis/context.py `_get_recent_errors()` now uses the same clustering | verified-offline (tests: tests/test_logtriage.py) | Clusters the WARNING/ERROR records in the last 400 lines of jarvis.log by logger and normalised message (paths → `<path>`, hex ids → `<id>`, numbers → `N`), attaches the untimestamped traceback lines to the record before them, drops the known noise (`NOISE`: the tools-registry budget warning — 650 of one day's 774 W/E lines — and the memory migrated-dir notice), and sorts by count, then ERROR over WARNING, then recency. Spoken: at most two sentences (the top two clusters with counts and the last time, then the ledger's outliers: turns that waited over 5 s and turns that never got an answer); the full list with tracebacks goes to the text card as a display-only `JarvisReply`. "Why was that slow" takes the LAST real ledger record (abort/superseded skipped — it may be hours old) and splits the wait into silence-before-stop / transcribing / working out the answer, naming the slow part, noting an energy-timer stop and a filler line; the route stage cannot be split into model vs tool time (no mark between them). The log line carries no date, so the window is a line tail, never a clock range (midnight-safe). Deterministic wording — no model call, so it never claims a check it did not do. |
| Claude permission approvals (broker + MCP prompt tool) | Automatic when a task needs a permission the project's settings.local.json does not cover: Jarvis speaks "Claude wants to run “git push origin main”, sir; shall I allow it?" and takes yes/no from typed, voice, the UI buttons or Discord | jarvis/approvals.py — ApprovalBroker (start/stop/pending/answer/auto_policy), ApprovalRequest, describe_request(), request_detail(), paths_in_input(), ask(); jarvis/mcp_permissions.py — PermissionServer (initialize/ping/tools/list/tools/call), ask_jarvis(), permission_result() | live (20 tests incl. a real UNIX-socket round trip and the real MCP server as a subprocess; the CLI contract itself verified against 2.1.247 on 2026-08-27) | Per-project `.claude/settings.local.json` is merged before every task (existing keys and user rules kept, Jarvis's allow/deny unioned in) plus `--permission-mode acceptEdits`; `--dangerously-skip-permissions` only when `claude.dangerously_skip_permissions` is true. Anything else would reach the MCP tool `mcp__jarvis__approve`: a dependency-free JSON-RPC-over-stdio server that opens `PATHS.LOG_DIR/approvals.sock`, sends one JSON line and waits 125 s for `{"behavior": "allow"|"deny"}`, returning Claude Code's permission-result shape; a missing socket denies with "Jarvis is not running". The broker auto-allows read/edit tools whose every path is inside `claude.allowed_dirs` (source "policy", nobody is disturbed), otherwise publishes `ApprovalRequested`, blocks the client, and denies after 120 s with "No answer in two minutes, sir; I've declined it."; `answer()` settles the named request or the oldest and publishes `ApprovalResolved`. The `--mcp-config` / `--permission-prompt-tool` pair is **verified** against CLI 2.1.247 (2026-08-27; the findings replaced the old TODO(fable) block at the top of jarvis/claude_session.py) and `claude.permission_prompt_tool` defaults **true**, so both flags are emitted on every task. That matters more now the pane is read-only: a permission ask leaves the pane and becomes a spoken question Jarvis can answer, whereas a question the TUI puts up in-pane cannot be answered at all. With the flag off, an out-of-project task is refused outright with OUTSIDE_LINE ("That folder isn't one you've cleared for me, sir, so I'd rather not touch it. Say the word and I'll open the terminal there instead.") and Claude Code auto-denies anything the allow list misses — a real fallback, not a silent one. The written allow list is workspace-scoped (`Read(/**)`, `Edit(/**)`, `Write(/**)`, …) and names the MCP server as `mcp__jarvis__*`: the bare `mcp__*` an older Jarvis wrote is invalid in 2.1.247 and stops an interactive session on a modal Settings Warning, so `STALE_ALLOW_RULES` prunes it on every merge. Tests: tests/test_mcp_permissions.py. |
| Local-vs-Claude routing (the router) | Every utterance that survives Tier 1 in jarvis mode: assistant vocabulary ("what's the weather", "what's on my calendar", "remind me…", "check my email", "tell me a joke") stays local; coding/dev/system vocabulary ("fix the failing test in the parser", "why is the build failing", "commit my changes and push the branch", "have claude tidy up the imports", a path token like `jarvis/brain.py`), skill phrases, "use opus/sonnet/fable", "think hard", "in parallel", "in the <name> project", "in the terminal, …" / "tell it to …" while the pop-out is attached, "cancel / stop that", "work on the <name> project", "pick up where we left off", "start a new project called <name>", "fast mode on/off" go to Claude or to a session action; anything genuinely ambiguous gets ONE spoken question | jarvis/router.py — Router.route()/resolve_answer()/pending(), RouteDecision, code_cues()/local_cues()/analyse(), estimate_size(), answer_kind(), DEFAULT_SKILL_PHRASES, ROUTER_QUESTION | live (rules verified offline; the model tie-breaker is stubbed in tests) | Eight pure rules in a binding order — session actions, **the open-terminal address (rule 1b)**, skill phrases (cfg.claude.skill_phrases regex → template with `$1`; `run the <name> skill on <args>` and a typed `/name args` pass through), explicit "claude" cues (stripped from the prompt), coding cues (verb×object, verb×path, question-about-code, "why is X failing", "step by step"), local cues, then the model. Strong cues beat weak ones ("fix the bug in the weather tool" is Claude; "the tests are slow" is only a weak cue); an utterance that opens with a wrapper imperative ("remind me to run the tests at 5") is local whatever follows; a Claude decision needs at least one cue and a bare ≤6-word sentence with none is local without asking. Rule 1b: `route(text, active_project, terminal_open=…)` takes `ClaudeSessionManager.terminal_open` as a callable (the commander builds it with `getattr`, so partial wiring means "no terminal" rather than a crash). `terminal_address()` recognises "in the terminal, do X", "in the <name> terminal, do X", "do X in the terminal", "tell it to X", "have it X", "ask it to X" — the pronoun forms need a coding verb after them, so "have it ready by noon" stays local — and when the probe says a terminal really is attached the decision is claude/reason="terminal" with `args["terminal"]=True`, which the commander answers with "Through to the <name> session, sir." (status "Terminal: <name>"). A session action still wins over the address, so "in the terminal, cancel that" cancels rather than queueing behind the task being cancelled; with nothing attached the same utterances route exactly as they did before. The tie-breaker is `brain.classify_route` — ≥0.75 confidence routes silently, below that the router speaks "Shall I hand that to Claude, sir, or is it a quick one for me?", remembers the utterance for 90 s and resolves it on the next reply (yes/claude/go ahead → Claude; no/just you/quick one/keep it local → local; a new subject drops the question). `estimate_size()` escalates two-or-more-object / "refactor" / "across" / "migrate" briefs to `claude.big_model` (fable). The router never speaks, publishes or touches a service. 60-utterance table in tests/test_router.py (20 local, 20 Claude, 10 actions, 10 ambiguous) routes as labelled with the model stubbed out; measured live, `classify_route` takes 3.3–6.9 s warm on this box, so with brain.py's 3 s cap the tie-breaker currently falls back to a question every time. |
| Router → service dispatch (the commander ladder) | Nothing to say: it is the order every utterance takes — dictation, then a ringing alarm's words ("stop / dismiss / okay / I'm up / enough", "snooze [N]"), then a pending Claude permission question (yes/no), then a pending router question, then desktop chains and the jarvis-prefixed registry, the voice intent gate (typed and Discord skip it), the unprefixed assistant Tier 1 (timer, reminder, alarm, schedule list/cancel, notes, to-dos, briefing) and finally Router.route() | jarvis/commander.py — Commander.handle(), _try_ringing/_try_approval/_try_router_answer/_try_assistant, _dispatch_router/_dispatch_route/_dispatch_action, _call_manager(), ASSISTANT_TIER1, REGISTRY | live (verified offline; 46 commander tests) | local → `brain.chat(text)` ("Thinking…"); claude → an ack generated by `brain.local_line` ("Right away, sir." on failure) then `claude.submit(prompt, project=…, parallel=…, model=…)`, a string back from the manager (queue/refusal line) being spoken verbatim; ask → the router question, spoken once; action → `claude.<cancel|work_on|resume|new_project|set_model|set_fast_mode>` with the argument bag filtered to the method's real signature (one attempt, so a TypeError raised inside the method can never resubmit the task), replying with the manager's own line, "Stopped, sir." for a cancel that stopped something. Every service is optional (`getattr`), so partial wiring degrades instead of crashing: no router or no `brain.chat` falls back to the legacy `brain.think`, no Claude speaks the setup line. Precedence is tested end to end: a ringing alarm beats a router action and a pending approval, an approval yes/no beats the registry, Tier 1 beats the router, and dictation beats all of them. `jarvis_agent.answer_question` now answers only ip / uptime / battery — weather and the clock belong to the local model's tools. |
| Local model tool loop (Tier 2 brain) | Any question the router sends local: "what's the weather like tomorrow", "what's on my calendar", "set a timer for fifteen minutes", "make a note that…", "what time is it in Tokyo", plus all chat | jarvis/brain.py — JarvisBrain.chat() / _chat_sync(), static_system(), build_user_turn(), configure(), set_registry(), ensure_resident() / start_residency(); jarvis/tools/registry.py — ToolResult, ToolSpec, ToolRegistry | live (gemma4:26b via Ollama; verified in-process on this machine) | `gemma4:26b` on `/api/chat` with the 11 tool schemas, `think:false`, `keep_alive:-1`, `num_ctx:8192`, at most 3 tool rounds and an 8 s wall budget; tool results are fed back and the final text is spoken through the same guards. The system prompt (persona + few-shots + tool rules) and the tool schemas are byte-identical for the life of the process and everything dynamic (time, window, git, last exchanges, memory facts) rides in the user turn, so Ollama's prefix cache hits: second call prompt_eval 0.13 s vs 0.35 s. `force_tool="get_briefing"` calls the tool first and renders it in one model turn. A `ToolResult.speak` skips the model entirely; a failing tool becomes an ok=False result the model explains; the registry never raises. Ollama refused -> Status(warn) + "I'm afraid my local model is down, sir." and never a silent fallback to Claude. Measured with the model resident alone (scratchpad bench_resident.md): tool schemas 749 tokens (bar 900), tool choice 11/12 and 12/12 on the one-tool prompts, simple answer model-side p50 0.55 s / p90 0.95 s (inside the 4.3 bar) but wall p50 2.42 s / p90 3.07 s — Ollama 0.30.11 spends 1.3-2.5 s per request in its own scheduler before the runner sees the prompt, on a 19-token prompt as much as on ours (scratchpad w5/probe_overhead.py, hooks_W5.md §3), so the wall bar is not reachable from this module. `classify_route` returns (route, confidence) from one JSON-schema call (5 s, not the spec's 3 s: 3 s timed out into a silent "local" on a third of the bench's questions); `summarize` ~3.2 s; `local_line` at its 2 s default almost always returns its fallback. Tests: tests/test_brain_tools.py (36), tests/test_persona.py. |
| Film-JARVIS persona on gemma4 (supersedes the llama3.2 row above) | Every spoken reply from the local model, plus Tier 3 [SPEAK]/[DONE] lines and autonomous updates | jarvis/brain.py — VOICE_RULES, FEW_SHOT_PINNED / FEW_SHOT_POOL, select_few_shots(), JARVIS_SYSTEM (tool rules + closing instruction), build_ollama_system(), build_user_turn(), spoken_from_ollama() | live (gemma4:26b; re-measured 26 Aug) | Same voice, three changes for the new model. (1) Tool rules in the static prompt: use a tool whenever the answer depends on live data and never guess it, call it without commentary, answer from the numbers in the result only, name anything that is "not set up", and never call a tool for a greeting, a joke or general knowledge. (2) The "no data" family is gone (weather and the clock are tools now) and the capability answer is the new truth — "I keep your calendar, weather, mail, reminders and alarms, sir, run the desktop, and hand the real coding to Claude". (3) The few-shot pool is down to greeting, joke and advice (two variants each) plus the three pinned lines: gemma4 lifts an example instead of learning from it — with the eight-family pool 6 of 14 eval prompts came back as a verbatim example (the pizza order answered with the text-my-brother line), and dropping the five families whose rule is already in prose, plus the closing clause "the examples are the manner only, never the words", took that to 0/14 (A/B: scratchpad w5/ab_parrot.json). Shipped numbers over the 14 base prompts (scratchpad persona_gemma4.md, harness persona_eval.py now on /api/chat with the tool schemas): 0 pool parrots, 0 riddles, 0 fake actions, 0 invented clock readings, 0 markdown/emoji/assistant-speak, 100% "sir", 100% at most two sentences, 93% exactly one. Tests: tests/test_persona.py (17). |
| Everything wired together (the services namespace) | Not spoken — it is the boot path behind every row above: `~/vss_env/bin/python -m jarvis.app` | jarvis/app.py — JarvisApp.__init__, _construct/_make_* factories, _register_tools(), _build_services(), start_assistant/stop_assistant, _on_brain_tags, the bus subscriptions | live (constructed headlessly in tests/test_app_wiring.py; 52 integration tests) | Construction order per spec 11: AssistantConfig.load() -> brain.configure(local_model) -> ToolRegistry -> Timekeeper(+legacy import) -> NotesStore -> ApprovalBroker -> ClaudeSessionManager -> Router -> Alerts -> DiscordChannel -> alerts.attach(discord) (which also switches the timekeeper's own notify-send off, one toaster) -> every tool module's make_tools(assistant, services) -> brain.set_registry. Every member is built through `_construct`, so an import or constructor failure logs and leaves that member None instead of aborting boot, and every consumer reads it with getattr (proved: a test that makes jarvis.router and jarvis.tools.mail unimportable still boots, still registers the other 15 tools, and still answers). services carries assistant, tools, brain(think/chat/classify_route/summarize/local_line/execute_autonomous), router, timekeeper, notes, claude, approvals, alerts, discord, plus desktop/context/memory/workflows/tts/reader/history and the two slots the tool modules fill (calendar, news_cache_path, spotify). Event fan-out: ClaudeProgress(milestone) -> speak + "milestone" alert; ClaudeTaskState done -> brain.summarize -> JarvisReply + speak + "done" alert, failed -> "blocked", cancelled -> status; ApprovalRequested -> speak + "question" alert (critical, no timeout); AlarmFired/ReminderFired -> alert; JarvisReply -> Discord echo while an exchange is active (deduplicated, 10 min). A BRIEFING tag turns that turn's SPEAK into ONE BriefingReady card and no JarvisReply. Measured tool-schema cost on gemma4 (prompt_eval_count, 26 Aug): the 11 spec tools = 897 tokens (bar 900); with the six Spotify tools registered = 1263 tokens, 17 tools against a MAX_TOOLS of 11 — over the spec 4.1 budget by design, the registry warns and keeps them. |
| Single-instance launcher (desktop / dock icon) | Click the Jarvis desktop or dock icon (or run `python -m jarvis.app` twice) | jarvis/app.py — _focus_running_instance(), _raise_window(), start_preload(), main() | live (unit-tested through the `_run`/`_sleep` seams; the click-to-window time still needs the UI item's restart to re-measure) | A second launch never starts a second Jarvis: the pid-file guard sends SIGUSR1 (deiconify from the tray) and then raises the existing window by WM_CLASS (`xdotool search --classname jarvis`, then windowmap + windowactivate + windowraise — the window is `-type splash`, which some WMs will not activate). If the first instance is still booting, it polls for up to 20 s and, failing that, toasts "Starting up, sir…" instead of exiting silently (the user's "neither of the jarvis desktop or taskbar buttons open jarvis": two clicks, both silent no-ops, log lines "Jarvis already running (pid …); focusing it"). The 15 s of blank screen that caused the second click is gone too: torch/CUDA/whisper/XTTS are imported on a worker thread AFTER the window is mapped (start_preload), not before create(), and the model loads still wait for the avatar's first full cycle — _load_models blocks on the preload event so the two never import in parallel. The .desktop files carry StartupNotify=true and StartupWMClass=jarvis so the dock shows a spinner and binds the window to the icon. |
| Config seeding and the test firewall | Automatic on first load; `JARVIS_ASSISTANT_CONFIG` / `JARVIS_MEMORY_DIR` / `JARVIS_CACHE_DIR` / `JARVIS_LEGACY_DIR` / `JARVIS_LOG_DIR` / `JARVIS_SPOTIFY_TOKEN` override every path | jarvis/config.py — PATHS; tests/conftest.py | live | PATHS gained the spec 3.2 entries: ASSISTANT_CONFIG, TIMEKEEPER_DB, NOTES_DB, CLAUDE_PROJECTS, CACHE_DIR, APPROVALS_SOCK, CLAUDE_TASK_DIR, MCP_CONFIG, AUTOSTART_DESKTOP, and MEMORY_DIR / LEGACY_AGENT_DIR became env-overridable so the suite cannot write into ~/.aiws_trainer. ~/.config/jarvis/assistant.json is seeded 0600 with placeholders only — including spotify{client_id, client_secret, default_device: HPCOMPUTER, liked_strategy, market} and discord{bot_token, channel_id, user_id} — and claude.permission_prompt_tool defaults false (the CLI permission flags stay off until they are verified live). conftest redirects the config, cache, memory, legacy and Spotify-token paths into a throwaway dir and asserts at session start that nothing points at /tmp/vss_voice, ~/.config/jarvis or ~/.aiws_trainer/jarvis_memory. MachineProfile.claude_bin falls back to ~/.local/bin/claude, so a GNOME autostart launch without that directory on PATH does not answer every Claude request with the setup line. |
| Assistant HUD surface (terminal button, WORKING pill, progress lines, PROJECT chip, alarm modal, briefing card) | Click the terminal button left of the mic; everything else is driven by events — ClaudeTaskState, ClaudeProgress, ActiveProject, ApprovalRequested/Resolved, AlarmFired/AlarmStopped, BriefingReady | jarvis/ui/views.py — CommandBar.set_terminal_state(), TranscriptView.add_progress()/add_approval()/resolve_approval()/add_briefing(), StatusStrip.set_project(), fmt_project_chip(), plan_strip(), progress_card_lines(), briefing_rows(); jarvis/ui/main_window.py — ClaudeTaskTracker, resolve_state(), terminal_button_state(project, session_seen, attached), terminal_tooltip(project, attached), session_exists(), terminal_attached(), alarm_modal_text(), Services.open_terminal/alarm_action/approval_answer/get_option/set_option | live (26 Aug, one restart: alarm modal + DISMISS/SNOOZE, approval card + DENY, briefing card, one Claude task, terminal button) | A second 44 px round button in the command bar, left of the mic and drawn in the mic's line style (4-arc CYAN ring, RAISED disc) with a terminal glyph; it opens or raises `gnome-terminal --title "Jarvis · <slug>" --geometry=200x50 -- tmux attach **-r** -t jarvis-<slug>` — READ-ONLY, so keystrokes in that window cannot drive the session — and is drawn FAINT with the tooltip "No project yet" until there is a session to attach to (a `tmux ls` a few seconds after boot, retried twice at 20 s while the answer is "none", and never polled after that — clicking a faint button still works and still answers "No Claude terminal to open" when there is nothing to open). Five drawn states: no_project / **open** / idle / working / waiting. The ring closes CYAN while a task runs, WARN while Claude waits on a permission answer, and also closes CYAN in the new `open` state — a terminal is attached to a jarvis-* session right now — where the tooltip reads "Claude's terminal is open — <project>" instead of "Open Claude's terminal — <project>". `terminal_attached()` is one bare `tmux list-clients -F '#{session_name}'` (no `-t`, no `-a`: tmux 3.4 rejects `-a` here) run off the Tk thread every 5 s, but ONLY once a jarvis-* session has been seen, so an app that has never run a Claude task still spawns nothing; the session manager answers the same question per project through `terminal_open()`. The header pill gains WORKING / WAITING from ClaudeTaskState (precedence speaking > listening > thinking > waiting > working > error > idle, so a spoken "Right away, sir." outranks WORKING); the status bar gains a PROJECT chip after the wake-word segment, shown only while a project is active, ellipsized to at least six characters — at 520 design px MEMORY yields to it (measured: wake 237 + PROJECT 130 + CPU 223 + GPU 209 + MEMORY 258 dev px against 1040). Claude milestones land in a compact mono/MUTED progress card that consecutive lines append to (12 visible, older ones collapse into "… N earlier steps"; any user or JARVIS card ends the run) — visibly distinct from spoken JARVIS cards. AlarmFired raises the window and centres a 300 px modal on the stage (label, time, DISMISS / SNOOZE 10) that hides only on AlarmStopped; the reactor keeps animating beneath. A briefing renders as exactly ONE card (label rows WEATHER / CALENDAR / NEWS, each label once, no duplicate JarvisReply). Settings gains an "Assistant" section: Morning briefing, the config path, Open Claude's terminal, Start at login. Measured on the live app: avatar cadence 30.3 fps, interval mean 1.97 std 0.40 ticks of 1/60 s (before: 2.00 / 0.51), idle late slots 0/900 over four consecutive 30 s windows. Tests: tests/test_ui_assistant.py, tests/test_hud_format.py. |
| Semantic long-term memory (facts by meaning) | "remember that my dentist is Dr Patel" (no wake word needed); "who's my dentist?" / "what did I say about the thesis last week?" (any phrasing reaches the model with the matching facts); "recall <topic>", "what did I say about <topic> [last week / yesterday / in the last three days]" | jarvis/memory.py — SemanticFacts (chromadb at `MEMORY_DIR/facts_index`, collection `jarvis_memory`, cosine), JarvisMemory.remember()/recall(query, since=)/relevant_facts()/format_for_context(text)/warm_index(); jarvis/brain.py — _dynamic_context(text); jarvis/tools/docs.py — _embed(keep_alive=); jarvis/commander.py — "remember" / "recall" in ASSISTANT_TIER1 | verified-offline + live read-only smoke (nomic-embed-text: "who's my dentist" 0.75, "what batch size" 0.87, unrelated ≤ 0.58; recall 10-20 ms with the embedder resident, warm-up 3.2 s) | facts.json stays the source of truth; every fact is also embedded locally (Ollama `nomic-embed-text`, the documents tool's seam, `keep_alive:-1` so it never cold-loads mid-turn) and the per-turn memory block is the top-3 facts RELEVANT to the utterance (floor 0.60, measured) instead of the last five in insertion order — nothing when nothing is close. Existing facts.json entries are indexed on first use / at start (`warm_index` on a daemon thread, only with residency). chromadb missing or Ollama down → substring recall only, one warning, 60 s back-off; nothing raises into a turn. `memory.semantic=false` turns the index off. "remember to …" is left to the notes tool. Tests: tests/test_semantic_memory.py (18). |
| People book (who "my advisor" / "Mom" is) | "my advisor is Dr Peyrovi, email hp@tamu.edu", "my mom is Linda", "remember that my TA is Sam Ortiz, his email is sam@tamu.edu"; "who's my advisor?"; "any email from my advisor?" (get_mail `sender`), "add lunch with Mom tomorrow at noon" (title expands to her name) | jarvis/memory.py — people.json, parse_person_statement(), add_person()/resolve_person()/expand_aliases()/format_people(); jarvis/commander.py — "person", "who is" (REGISTRY + ASSISTANT_TIER1, ahead of "remember"); jarvis/tools/mail.py — get_mail(sender=), sender_matches(), resolve_sender(); jarvis/tools/calendar.py — expand_people() at the add_event title | verified-offline | A "my X is Y" sentence is a contact only with one positive sign (an address, a title, a relation word for X, or a capitalised multi-word name in the transcript), so "my favourite colour is blue" still reaches the model. The People block rides every turn OUTSIDE the relevance-ranked fact window, so a contact never drops out of context. Mail: an alias resolves to the stored address (else the name) and filters the latest 60 messages; "no mail from Dr Peyrovi" is a fact for the model, not the empty-inbox line. Calendar: the alias is replaced in the title before the write or the read-back. Tests: tests/test_people_book.py (27). |
| Activity journal + spoken day recap | "recap my day", "give me a recap of my morning", "what was I doing before lunch?", "what did I get done this afternoon?", "what have I been working on the last two hours?", "what did I do yesterday?" | jarvis/context.py — ContextEngine._journal_write()/journal_tool()/journal_claude()/journal_window()/journal_rows() (one JSON line per event in `MEMORY_DIR/journal/YYYY-MM-DD.jsonl`); jarvis/tools/journal.py — parse_window(), digest(), ActivitySampler (services.activity_sampler, started/stopped by the app), recap_day tool; jarvis/brain.py — _journal_tool() in the tool loop; jarvis/app.py — Claude done/failed rows, services.context_engine; jarvis/commander.py — "recap" (force_tool=recap_day) | verified-offline; live sampler pending a desktop session | Every exchange (untruncated, 4 000-char cap), tool call, Claude task result and window-focus change is journaled; the sampler reads the focused window through the engine's cached xdotool probe every `journal.window_interval_s` (60 s), journals only changes, skips a locked/empty desktop, and feeds "go back". A recap pins `recap_day`: the rows for the window are bucketed by hour into a plain-text digest bounded to 3 200 chars (window lines go first, then snippets shorten, then the oldest hours collapse), the model speaks at most four sentences from it and the whole digest goes on a card (`JarvisReply`, display only). Day files older than `journal.keep_days` (90) are pruned at start. Tests: tests/test_journal.py (35). |
| Explain a document, then read it (student) | "explain the biosensors lab handout", "summarize the CS101 syllabus", "walk me through the biosensors PDF", "give me a summary of report.pdf"; then "read it to me" / "read the document" reads the same file in parts ("continue reading" for the rest); "read file handout.pdf" reads a PDF/.docx directly | jarvis/reader.py — DOC_SUFFIXES, unwrap_text(), ReadAloud.resolve_document() / document_text() / read_document(); jarvis/tools/docs.py — match_name(); jarvis/brain.py — explain_text() (EXPLAIN_FORMAT, no tools); jarvis/commander.py — explain_kind(), _h_explain_doc, _READ_PRONOUN_RX, "document" read kind; jarvis/app.py — _async_reply (services.reply), ReadAloud search_dirs += docs.paths | verified-offline (fake pdftotext / fake _http; live summary quality and latency unmeasured) | A spoken name resolves fuzzily against the file names in cwd / ~ / ~/Jarvis / `docs.paths` (one folder deep; content words, number words as digits, ≥60 % of the words must match, so "the lab report" never picks the biosensors LAB handout). Text comes from tools.docs.extract_text (pdftotext / stdlib docx) and is unwrapped first (hard line breaks and form feeds → prose) so the reading is not choppy. "Explain" acks at once ("Let me have a look at X, sir."), then ONE tool-free JSON /api/chat (static system prompt, first 12 000 chars, num_predict 420, 60 s) returns a two-sentence spoken lead — spoken with "Say 'read it to me' for the whole thing" — and a fuller paragraph shown as a display-only JarvisReply card. The worker's reply goes through services.reply, which closes the turn and arms the follow-up window like a brain reply, so "read it to me" needs no wake word; the pronoun form is diverted to the document only for 15 min after an explain ("read this" / "read the selection" never are). The regex is narrow on purpose: a verb of explanation plus a document word — strong words (handout, pdf, document, syllabus, textbook, worksheet, slides, lecture notes, rubric, reading, an extension) speak an excuse when nothing resolves, weak ones (file, paper, report, article, essay, manual, assignment, spec, chapter) fall through to the router, and "summarize my inbox" is untouched. Excuses: "I can't find a document called X, sir.", "I couldn't get any text out of X, sir.", "I'm afraid I couldn't make sense of that document just now, sir."; no model → offers to read instead. Tests: tests/test_explain_doc.py. |
| Quiz me from my documents (Leitner flashcards) | "quiz me on chapter three", "test me on the syllabus", "drill me over the biosensors handout"; answer in the follow-up window; "skip" / "I don't know" reveals the answer; "stop the quiz" gives the tally; "review my flashcards" / "flashcards" asks what is due | jarvis/tools/quiz.py — FlashcardStore (sqlite MEMORY_DIR/flashcards.db, boxes 1-5, BOX_DAYS 0/1/3/7/14), QuizSession, grade_by_string(), study_text(); jarvis/tools/docs.py — DocsIndex.names() / chunks_of(), topic_chunks(), services.docs parked by make_tools; jarvis/brain.py — make_quiz() (QUIZ_FORMAT), grade_answer() (GRADE_FORMAT, 8 s); jarvis/commander.py — _QUIZ_RX / _REVIEW_RX / _QUIZ_STOP_RX / _QUIZ_SKIP_RX, _h_quiz, _h_review, _h_quiz_stop, Commander._pending_quiz, _try_quiz_answer; `quiz.questions` (5), `quiz.chunks` (6) | verified-offline (real chromadb + fake embedder, fake model calls; live question quality unmeasured) | Topic retrieval prefers a FILE NAME match (no embedding call), starting at a "chapter N" heading when the topic names one, then a chapter heading anywhere in the store, then the embedding query with hits under 0.3 dropped and the rest re-sorted into reading order — "chapter three" is a poor semantic query. Generation is one tool-free JSON /api/chat (≤6 000 chars of chunks, num_predict 110/question, 45 s) on a worker thread behind the ack "Let me put some questions together on X, sir."; the first question arrives via services.reply so the follow-up mic opens. The NEXT utterance is the answer: the quiz rung sits in handle() ahead of the intent gate (which calls "forty percent" background chat) and all state lives in the commander, never app._pending_uncertain. Grading is the normalised-string match first (number words → digits, plurals/filler out; every expected content word present → right, no overlap or a different number → wrong), the model only for the unclear ones, and a shrug that names the answer when neither can tell (no Leitner move). Right → box up, due in BOX_DAYS; wrong → box 1, due now. A question older than 5 min is stale (the text routes as new); a new "quiz me" replaces the session; "quiet" ends it silently; "stop the quiz" with no quiz open is the model's. Excuses: NO_DOCS_LINE, NO_TOPIC_LINE, NO_QUESTIONS_LINE, INDEX_DOWN_LINE, INDEXING_LINE (an empty store with files present kicks the index), NOTHING_DUE_LINE, NO_CARDS_LINE. Tests: tests/test_quiz.py. |
| Quiet hours, do-not-disturb and the catch-up digest | "Do not disturb for an hour" / "don't disturb me until seven" / "hold my notifications for two hours" / "I'm busy for the rest of the day"; "quiet hours from eleven to seven" / "turn off quiet hours" / "what are my quiet hours"; "I am free" / "what did I miss"; "are you on do not disturb". Automatic while a timed calendar event titled class / exam / meeting / busy is running (`quiet.calendar_keywords`), during `quiet.hours`, and while the phone is away (`quiet.hold_when_away`) | jarvis/quiet.py — QuietPolicy (reason/is_quiet/should_hold, hold/release/digest, set_dnd/set_hours/free, the 30 s tick thread), parse_clock; jarvis/app.py — _say(proactive=, kind=), _make_quiet, _briefing_due; jarvis/commander.py — _h_dnd/_h_quiet_hours/_h_free/_h_quiet_status; jarvis/tools/timekeeper.py — _speak(proactive, kind); jarvis/channels/notify.py — set_quiet_gate | live (verified offline: tests/test_quiet.py, 62 tests incl. the real JarvisApp build) | Only PROACTIVE speech is held: watchdog warnings, reminders and timers, meeting heads-ups, the hooks narrator (speak_queue). Alarms ring and speak regardless (the timekeeper tags them non-proactive); answers, approval questions and Claude task lines are never touched — the gate is the caller's `proactive=True` flag, not `_say` itself. Held lines (cap 12) come back as "While you were busy, sir: two reminders and one warning. …" when the window closes (the policy's own tick), on "I am free", or after "Welcome back, sir". Desktop banners are silenced in-process through notify.desktop_banners_enabled() (no gsettings: a crash mid-window would leave the desktop mute). The first-wake briefing waits for the window to close. "quiet" alone is still the barge-in. DND persists in assistant.json (`quiet.dnd_until`); "I am free" overrides the rest of the current window only (`quiet.free_until`). |
| Presence sentinel (is Hunter home?) | Automatic once `presence.phone_ip` and/or `presence.phone_mac` are set; nothing to say. On the phone's return after an absence: "Welcome back, sir." plus "While you were out, sir: …" for anything held | jarvis/presence.py — probe(ip, mac, run) (`ip -4 neigh` REACHABLE/DELAY → present; STALE/absent → `ping -c1 -W1`), parse_neigh, PresenceSentinel (tick/start/stop, is_home(), state); jarvis/events.py — Presence(home, since, returned); jarvis/app.py — _make_presence, _on_presence; services.presence | live (verified offline: tests/test_presence.py, 20 tests; probe checked read-only against the live ARP table 2026-08-30: STALE→ping→True, MAC-only→True, dead IP→False) | Wi-Fi only: no phone is paired to the Spark and a phone does not stay Bluetooth-connected to a Linux host, so there is no BT leg. STALE is never absence (every neighbour but the router shows STALE between conversations). Away has hysteresis: unseen for `presence.away_after_min` (12) before he is "out" — iPhones nap off Wi-Fi for minutes; home is immediate. The grace clock starts at boot, so a phone asleep at startup earns no spurious welcome. is_home() is True until the probe has established otherwise (a misconfigured probe must not mute him). Consumers: jarvis/quiet.py (hold_when_away) and the app's greeting; leaving is silent. Polls every `presence.poll_s` (60) on a daemon thread joined on stop. |
| Nightly self-review ("how did yesterday go") | "how did yesterday go / how was yesterday / yesterday's review / what went wrong yesterday", "how's today going / how did today go" (Tier 1, with or without the prefix); spoken automatically as its own line before the first-wake briefing; the full table posted to Discord when that channel is configured | jarvis/dayreview.py — summarize_day(), spoken_line(), table(), DayReviewer (files MEMORY_DIR/reviews/<date>.json every quarter hour for any day that has ended); jarvis/app.py — _review_line(), day_review_text(), _on_review_filed(), the first-wake hook in _deliver_first_wake_briefing; jarvis/commander.py — _DAYREVIEW_RX, _h_dayreview, REGISTRY "day review" (+ ASSISTANT_TIER1) | verified-offline (the live log has 8 ledger records so far; first real digest lands the night after the restart) | Pure and local: reads jarvis.log(.1/.2) + turns.jsonl, no model call (every number is kept, by construction). The log has no date, so the file is sliced where the clock runs backwards and each slice is dated from the ledger's epoch stamps; within a day only lines after the app's own boot marker ("tools registered:") count, which is what keeps a pre-firewall test traceback ("cuda gone") out of the tally. Counts: turns, median/worst wait, aborts, uncertain / ignored, speaker-gate rejections (a whole clip dropped: "segment filter: 0/N" + FAILED SHUT; the raw "speaker verify … REJECT" count is table-only because the wake check and the recorder's 1 Hz poll log it too), wake words refused, watchdog releases, tool-handler exceptions (registry "tool X failed" + commander "handler X failed"), TTS fallbacks ("falling back to", "fish retired"), model reloads (a load ≥ 0.5 s more than 3 min after a boot), boots, ERROR/WARNING totals, the top six error messages. Reviews are kept 60 days under MEMORY_DIR (LOG_DIR is tmpfs and is wiped at boot). Discord: kind "milestone" through Alerts, so no desktop banner. Tests: tests/test_dayreview.py (24). |
| Command socket + `jarvis` CLI (ask him from a shell, SSH, tmux, a script) | `jarvis "what's due this week"`, `jarvis -q "how did yesterday go"` (text only), `jarvis --status`, `ssh spark jarvis 'timer 5 minutes'`, `python -m jarvis.ask --json …`; scripts/jarvis is the wrapper | jarvis/cmdsock.py — CommandSocket (UNIX SOCK_STREAM at PATHS.COMMAND_SOCK, 0600, thread per client, modelled on approvals.ApprovalBroker), ReplyCollector, ask(); jarvis/ask.py — the stdlib client; jarvis/app.py — dispatch_text(text, source="cli", quiet=…), _say's per-turn quiet gate, start/stop in start_assistant/stop_assistant | verified-offline (real sockets in tests, incl. one round trip through the real JarvisApp) | One JSON request line in, JSON lines out: reply / status / end. A turn's end has to be inferred (no turn id; _turn_finished fires at once for every non-voice source): closes on the sync reply for a done result, on BrainState idle after thinking, on the first reply while the model is not thinking, after 3 s of quiet for a done=False ack (a Claude task: the ack is the answer), or on the timeout (90 s default, cap 600). "status" answers diagnostics_text() without a dispatch. Speech: on by default like a typed command; -q suppresses it for that turn only (a concurrent voice turn resets the gate). The question shows in the transcript as UserUtterance(source="cli"); it is not added to the typed-box history. Exit codes: 0 replied, 2 Jarvis not running, 3 no reply (timeout / task ack). Tests: tests/test_cmdsock.py (19). |
| Git standup across every cleared project | "what did I do today / yesterday", "what did I change today", "what have I done today", "what did I work on yesterday", "standup", "daily standup", "yesterday's standup", "give me the standup" (Tier 1, with or without the "jarvis" prefix); "git status" now reports the Jarvis repo | jarvis/standup.py — STANDUP_RX, day_word(), window(), repo_dirs(), sessions_on(), is_project_session(), spoken_line(), card_text(), build(); jarvis/context.py — probe_repo(), ContextEngine.git_repos() / git_summary() / repo_dirs; jarvis/commander.py — _h_standup (REGISTRY + ASSISTANT_TIER1); jarvis/app.py — context.repo_dirs from standup.default_repo_dirs(assistant), context_ns.git_summary → the V3 probe | verified-offline (tmp git repos + a tmp ~/.claude/projects; one real-app build) | Walks `claude.allowed_dirs`, the children of `claude.projects_root` (tolerated when missing) and `~/vss_env`, primary first; per repo `git log --since/--until` for the day's subjects (ISO window, `--no-merges`), `status --porcelain` count, `diff HEAD --shortstat`, branch and ahead-of-origin. Claude Code sessions touched that day come from `~/.claude/projects/*/*.jsonl` by mtime, parsed up to 1 MB each (title/summary + opening line sit at the top), with scratchpad / probe / subagent transcripts dropped (cwd under /tmp, ~/.claude or $HOME itself). Output: a display-only card of commit subjects per repo (12 per repo, then "… and N more", plus the uncommitted stat and the session titles) and TWO spoken sentences composed from the data in his voice — "Today you made 4 commits in Jarvis and haymaker-digest, sir, with 3 files in Jarvis still uncommitted. Claude had 2 sessions today, on Standup feature and TTS shootout." — no model turn, so it answers while the GPU is lent out. Synchronous (well under a second): a done=False turn would sit open until the 60 s watchdog. The prompt's own "Git:" line is unchanged: ContextEngine._fetch_git still renders ONE dict from the first repo, so the Tier 2 static prefix does not move. V1 `JarvisAgent.git_summary` (hard-coded to ~/vss_env, so "git status" used to report the VSS tree) is no longer wired. Tests: tests/test_standup.py. |
| GPU yield-and-reclaim (lend the local model to a trainer) | Automatic when `health.yield_to_trainer` is true (default false): a training process appears → "I have lent the GPU to your trainer, sir; quick answers only until it is done."; gone for two ticks → "Your trainer has finished, sir; I'm loading my model again." Manual (Tier 1): "lend the GPU (to the trainer)", "release / free up the GPU", "unload your model"; "take the GPU back", "take back the GPU", "reclaim the GPU", "load your model back" | jarvis/brain.py — release(), reclaim(), is_lent(), ModelLent, MODEL_LENT_LINE, _RESIDENCY["lent"], _chat_payload keep_alive, ensure_resident() gate, _chat_sync gate, _persona_request / classify_route _check_lent(); jarvis/tools/health.py — is_trainer(), find_trainers(), Snapshot.trainers, TRAINER_HINT_RX, TRAINER_ABSENT_TICKS, Watchdog._trainer_rule() / manual_reclaim() / lent_to, LENT_LINE, RECLAIMED_LINE; jarvis/commander.py — _h_gpu_lend / _h_gpu_reclaim (REGISTRY + ASSISTANT_TIER1); jarvis/app.py — brain_ns.release/reclaim/is_lent; jarvis/assistant_config.py — health.yield_to_trainer | verified-offline (FakeOllama + fake /proc; the state machine driven through Watchdog.check) | release() is the same `POST /api/generate {model, keep_alive: 0}` ensure_resident makes for foreign models, then a `lent` flag: every local door — the tool loop (sync AND streamed, forced tools included), summarize / local_line (fall back to their text), classify_route (("local", 0.0)) — returns before any HTTP, because every payload used to pin the model back with keep_alive −1 and the first local turn would have reloaded 26B (6.9 s) behind the trainer's back; any payload still built while lent says keep_alive 0. The residency loop keeps ticking but never re-warms while lent; reclaim() clears the flag and runs ensure_resident (7 s+, off the caller's thread in the commander: the acknowledgement speaks first, a failed warm-up is spoken after). Trainer detection keys on the COMMAND LINE, not size (a small-batch run under hog_gb would never trip the hogs rule): interpreter-named processes only (python/uv/torchrun/accelerate/deepspeed), a `train|trainer|finetune|fine_tune|pretrain` word in the script or -m module ("constraint.py" does not count), own pid excluded. Hysteresis: two absent ticks (60 s) before reclaiming so an epoch restart does not cost a reload; a replacement trainer inside the same tick keeps the loan. "Take the GPU back" while the run continues is an order: Watchdog.manual_reclaim() remembers that pid and will not lend to it again (a different trainer alongside it still gets the GPU). The flag is set even when Ollama is down (the point is that nothing reloads it); an unload that fails is reported in the status chip. Cost while lent: every tool answer (weather, calendar, mail, notes, Spotify, docs, screen, Canvas, briefings) is the lent line; Tier 1 (clock, timers, notes, todos, standup, GPU commands) and the Claude / web routes still work. Tests: tests/test_gpu_yield.py. |
| Route short-cut: the router already named the tool | Automatic for read-only calendar questions ("what's on my calendar tomorrow?", "when's my next meeting?", "do I have any meetings on Monday?"), weather ("what's the weather tomorrow", "what's the weather like in London?", "will it rain this week?") and time-in-a-city ("what's the time in London?") | jarvis/commander.py — forced_call(), place_in(), calendar_range(), weather_when(), _CAL_READ_RX / _CAL_WRITE_RX / _CLAUSE_RX / _PLACE_RX; wired in Commander._dispatch_route (local branch) -> brain.chat(force_tool=, force_args=) | verified-offline (tests/test_route_shortcut.py: real Router, mocked brain; the live timing that motivated it is from the 2026-08-29 log) | When the router's reason is local:calendar / local:weather / local:clock and the sentence carries the arguments, the tool is forced and only the RENDER model turn runs — the tool-choice turn (0.98-1.45 s live) is gone. The render turn is kept on purpose: brain.py never speaks tool text raw (a calendar title is a stranger's words). Deliberately narrow: a write verb ("book/schedule/put/add ... meeting", start-anchored so "what's on my schedule" is a read), a second clause (", and", "and", "also", "then"), a lowercase city ("in london" typed — Whisper capitalises places it knows) or a local:clock with no city ("what year is it") all fall back to the full tool loop: slower, never wrong in a new way. Home-clock questions never reach this (Tier-1 _h_clock). |
| Corrections: "no, I said …" | "no, I said set a timer for ten minutes", "I meant the calendar", "nope, I mean what's the weather", "that's not what I said, I said …", "not the terminal, the calendar" (comma required) — typed, on Discord, or by voice within 60 s of the last turn (the follow-up window or the wake word) | jarvis/commander.py — correction_kind(), _CORRECTION_RX / _CORRECTION_NOT_RX, Commander._try_correction (stage 2b, ahead of every yes/no stage), CommandResult.corrected, Commander._last_turn; jarvis/brain.py — job generation (_stale(gen), chat()'s _live()); jarvis/context.py — ContextEngine.forget_exchange; jarvis/memory.py — log_correction/get_corrections (corrections.json); jarvis/app.py — services.conversation, brain_ns.cancel, _dispatch/_after_dispatch record under the corrected text; opt-in vocab: new_vocab_words() + corrections.learn_vocab | verified-offline (tests/test_corrections.py incl. the real JarvisBrain with _chat_sync stubbed) | The meant text is re-dispatched through the whole ladder with the intent gate bypassed (a correction is addressed to Jarvis by construction — the classifier would call "the calendar" background chat). Speech is cut, the misheard turn's model job is cancelled and its generation goes stale so its reply is neither spoken nor remembered even after the corrected job starts (the shared _cancelled flag alone was reset by the next acquire), the misheard exchange leaves the model's window, and (heard, meant) goes to ~/.aiws_trainer/jarvis_memory/corrections.json. Runs BEFORE the approval / event / router yes-no stages, which read any sentence opening with "no" as a decline. By voice it needs a turn in the last 60 s to correct, else "not that one, the other one" is the room talking and goes to the classifier as before. `corrections.learn_vocab` (off) appends new capitalised words from the correction to the Whisper vocabulary prompt (~/.aiws_trainer/voice_vocab.txt, 900-char cap): Whisper's casing on a misheard name is itself a guess, so it is opt-in. |
| Voice feedback: "that was for you" / "that wasn't for you" | After a command he dropped as background chat: "that was for you" (within 20 s; the wake word or the prefix both fine) re-runs it. After a reply he did not ask for: "that wasn't for you" / "I wasn't talking to you" / "not you" (within 60 s) stops him. Both label the last turn. After "Was that for me?": "that was for you" now also answers the spoken 5 s window | jarvis/commander.py — feedback_kind(), _FEEDBACK_YES_RX / _FEEDBACK_NO_RX, Commander._try_feedback (stage 3e, before the classifier), Commander._feedback_line (feedback.jsonl), Commander.claim_uncertain hook, parse_yes_no yes-phrases; jarvis/app.py — _claim_uncertain | verified-offline (tests/test_voice_feedback.py: real IntentClassifier on a tmp log; the learned n-gram is checked to change a fresh classifier) | Feeds the classifier's own log (IntentClassifier.log_feedback -> ~/.aiws_trainer/intent_log.json, the one classify() reads — not a third intent log) plus one audit line per label in ~/.aiws_trainer/jarvis_memory/feedback.jsonl {ts, text, prev_status, label, how}. The re-run bypasses the classifier (it just said no). A "wasn't for you" after a reply cuts speech and forgets the exchange from the conversation window. Stale feedback gets "I'm not sure which one you mean, sir." rather than falling into the classifier. Router misroutes are logged, not learned: the router is rule-based. Known limit: after "Was that for me?" the turn stays open (done=False), so a wake-worded answer after the 5 s window waits on the 60 s turn watchdog — the card and the spoken window remain the paths that work. |
| Destructive read-back before a bulk cancel or list wipe | "cancel all my alarms" / "clear all timers" with more than one item -> "Cancel all three alarms, sir?"; "clear my list" with more than one to-do -> "Clear all three to-dos, sir?"; then "yes" runs it, "no" or any new subject drops it. A shaky transcript (avg_logprob under `confirm.shaky_logprob`, -0.7) is read back even for a one-item whole-list cancel ("Cancel the alarm, sir?"). "cancel the timer" / "stop the timer" / "cancel that" are never read back | jarvis/commander.py — _h_cancel_schedule + _do_cancel_schedule, _wants_read_back, Commander.stash_destructive / shaky_transcript / _try_destructive_confirm (stage 3d), handle(confidence=); jarvis/tools/notes.py — the notes tool's remove/all sets NotesStore.pending_clear and reads back; jarvis/app.py — _process_audio -> _dispatch(confidence=) -> handle(confidence=); jarvis/assistant_config.py — `confirm.read_back`, `confirm.shaky_logprob` | verified-offline (tests/test_destructive_readback.py: mocked timekeeper, a real NotesStore through the real tool, the app's confidence plumbing) | The yes/no is VAD-timed for free: the read-back is a spoken reply, so _after_dispatch opens the follow-up window and the "yes" comes back through handle(). Anything that is not a clear yes/no drops the offer (changing the subject is not consent), and an offer older than 60 s is dropped even on a yes. Threshold calibrated from the live log (n=32 accepted turns: p10 -0.61, median -0.41; the garbled ones at -0.94/-0.95), so -0.7 flags the doubtful tail without nagging. Confidence travels only when there is one: typed text passes none, and a stand-in commander need not take the keyword. Out of scope by design: "close window" (desktop chain, runs in the background) and swapping _ask_uncertain's record_fixed(5 s) for the VAD follow-up (it deliberately keeps its reply out of routing — a separate change). |
| Local voice: resident F5 sidecar (systemd) + loud local fallback | `scripts/setup_f5_service.sh` (copies the unit, daemon-reload, enable — never starts it), then `systemctl --user start jarvis-f5.service`; automatic thereafter: Jarvis adopts the unit's socket, and on the fish engine warms it at load | scripts/systemd/jarvis-f5.service, scripts/setup_f5_service.sh; jarvis/tts.py — F5_UNIT, _f5_unit_active(), _ensure_f5_server() (adopt → wait for the unit → spawn), _wait_for_f5_unit(), retire_own_f5_sidecar(), TTS.warm_f5_fallback(), TTS._f5_unavailable(), TTS.release_f5_sidecar(); jarvis/app.py — quit() | verified-offline (sidecar stubbed in tests; the unit's exact ExecStart argv was run once against a scratch socket on 2026-08-30: model resident in 3.2 s from a warm HF cache, tts.py adopted it without a Popen, "Always, sir." rendered in 0.35 s wall with the duration floor engaged, 1.0 GB GPU; the live cutover — enabling the unit and flipping tts_engine to f5 — is a manual step) | User unit (`Restart=always`, `WantedBy=default.target`, no sudo) runs `~/.local/share/jarvis-f5/venv/bin/python scripts/f5_server.py` against the same socket and reference paths as PATHS (F5_SOCK, VOICE_REF_F5, VOICE_REF_F5_TEXT; ExecStartPre recreates /tmp/vss_voice because /tmp is wiped at boot), so the ~180 s model load is paid once per boot, not per outage. `_ensure_f5_server` refuses to Popen while `systemctl --user is-active jarvis-f5` says active/activating — it waits for the unit's socket instead (re-checking the unit every 5 s so a dead unit fails fast) — because f5_server.py unlinks and rebinds the socket on start and two copies would fight over it. With `tts_engine=fish` the fallback is warmed on a daemon thread at load() (once per TTS instance) and a warm-up failure is a Status(warn) plus a log line rather than silence. With `tts_engine=f5` an unavailable sidecar no longer drops silently to edge (Microsoft's cloud): it logs at error, publishes Status(warn) "F5 voice down — using XTTS (local)" and loads XTTS when its reference clip exists, and only otherwise falls to edge, announced as the cloud voice; load() returns the replacement's verdict so the utterance is not dropped. At quit a Jarvis-spawned sidecar is terminated only when the unit is active (it has superseded ours); without the unit it is left resident for a warm next launch, as before. Tests: tests/test_f5_service.py (unit paths vs PATHS, is-active parsing, no-Popen guard, wait/timeout, warm thread, loud fallback order, quit hook, the install script against a stub systemctl). |
| Your OWN Claude Code sessions narrated (tests, prompts, done) | Automatic once the hooks are installed: `~/vss_env/bin/python scripts/claude_hooks/install.py` (run it yourself; nothing in Jarvis calls it). In any of your own `claude` terminals a pytest/ruff run says "3 tests failed in Jarvis, sir." / "Tests passed in haymaker digest, sir.", a permission or idle prompt says "Claude needs your say-so in <project>, sir." / "Claude is waiting on you in <project>, sir.", and a long turn (45 s+) or one that ran tests ends with "Claude has finished in <project>, sir." Off switch: `alerts.claude_hooks: false` | scripts/claude_hooks/narrate.py — main(), line_for(), jarvis_alive(), hooks_enabled(); scripts/claude_hooks/install.py — hooks_snippet(), merge(), remove(); jarvis/claude_session.py — DRIVEN_ENV, build_command() | verified-offline (hook driven through main() in tests; the live settings.json merge is a manual step) | Stdlib-only hook (starts in ~30 ms) fed the hook JSON on stdin by Claude Code (PostToolUse:Bash, Notification, Stop, UserPromptSubmit); appends one line to PATHS.SPEAK_QUEUE, never stdout. Silent when JARVIS_DRIVEN is set (every pane Jarvis launches carries it on the launch line, since those are already narrated from the transcript), when /tmp/vss_voice/jarvis.pid is absent or stale, or when alerts.claude_hooks is false (read with plain json). Stop fires every turn, so it is gated on a test run or MIN_TURN_S; repeats are limited per session (30 s, 120 s for idle prompts) via a tiny state file under ~/.cache/jarvis/claude_hooks. The test-outcome regexes are COPIES of claude_session's; tests/test_claude_hooks.py fails on drift. |
| Personal Whisper vocabulary (his words, not the warehouse's) | Automatic on every utterance — nothing to say. Teach it with "add X to your vocabulary", a spelled name, a correction (`learn_vocab`), "pronounce X as Y", or the vocabulary editor | jarvis/vocab.py — build_prompt(), load_names(), add_name(), clear_cache(); jarvis/transcriber.py — DEFAULT_VOCAB (assistant seed), Transcriber(prompt_provider=), _prompt(); jarvis/tools/canvas.py — cached_course_names() | live | initial_prompt built per turn, priority order: voice_vocab.txt (manual + corrections) → voice_names.txt (newest first) → his TTS pronunciation entries → the seed → calendar cache titles → cached Canvas courses; deduped, capped at 880 chars (~220 tokens, Whisper's prompt window), cached 60 s, cache busted the moment a name is taught. Read-only harvests: the calendar disk cache and the Canvas module cache are never fetched for. Replaces the VSS warehouse list ("AGV, forklift, pallet…") that primed every utterance |
| Name spelling in both directions (Whisper + TTS) | "jarvis, my advisor's name is spelled P-E-Y-R-O-V-I, say it pay-ROH-vee"; "the name is spelled P E Y R O V I"; "add Librespot to your vocabulary" (Tier 1; prefix optional in jarvis mode) | jarvis/commander.py — _SPELL_NAME_RX, _ADD_VOCAB_RX, spelled_word(), _h_spell_name, _h_add_vocab | live | Letter runs normalised ("P E Y R O V I", "P.E.Y.", glued "PEYROVI" all → Peyrovi); mixed multi-letter tokens are refused rather than guessed. The name lands in ~/.aiws_trainer/voice_names.txt for Whisper; a "say it …" clause also lands in the TTS dictionary exactly as "pronounce X as Y" would. The reply echoes the reconstructed spelling and the word, spoken through the new pronunciation — the confirmation loop |
| Sticky modes stay on the microphone (voice-CLI handoff) | Nothing to say — automatic. With lecture notes, dictation or a quiz open, `jarvis "what's due today"` from a shell (or a Discord message) is answered normally instead of being filed/typed/graded. Close a mode from anywhere with its end phrase ("end notes", "end dictation", "stop the quiz"); file a deliberate line with `jarvis "note: the demo is on friday"`; see what is open with `jarvis --status` | jarvis/commander.py — _handle_inner rungs 1a/1b, _dictation_end(), _lecture_end(), _note_prefix()/_NOTE_PREFIX_RX, _handle_lecture(note=), _try_quiz_answer(source); jarvis/app.py — open_modes_line(), diagnostics_text() | live | The modes are stuck to the mic because that is the only channel that cannot address them: a spoken sentence during a lecture has no other meaning. A socket turn does — it was typed on purpose — so source != "voice" routes normally. Escapes both ways: the end phrases and the quiz stop words are honoured from ANY source (a terminal must be able to close a mode), and "note: …" files a verbatim line (so "note: end notes" writes a line rather than closing). A ghost lecture flag (file lost) is still recovered from any source. `open_modes_line()` is appended to the diagnostics line, e.g. "Lecture notes open for BIOSENSORS, 12 lines." |
| Longer follow-up window while a question is open | Nothing to say — automatic. After a flashcard question, a destructive read-back ("Cancel all three alarms, sir?") or the good-night wake-alarm offer, the mic stays open `quiz.window_s` seconds (default 15) instead of the usual 4 for the answer | jarvis/app.py — _capture_window(), _window_setting(), _question_open(); jarvis/assistant_config.py — quiz.window_s; jarvis/recorder.py — clamp_window() | live | The 4 s CONFIG.followup_window is sized for "...and Tuesday?", not for thinking about a card: mid-quiz nearly every answer needed the wake word again, though QuizSession.ANSWER_WINDOW_S is 300 s. Lecture notes still win (lecture.window_s, 20 s). Each source is checked live — a finished or stale quiz, a read-back past DESTRUCTIVE_TTL_S and an offer past OFFER_TTL_S all fall back to 4 s — so the mic is never held open by a question nobody is answering. The recorder clamps the request to MAX_FOLLOWUP_WINDOW_S (30 s), and every follow-up is still speaker-verified |
| Confidence-aware confirmations for alarms, timers and reminders | Nothing to say — automatic. When the transcript scraped in under `confirm.shaky_logprob`, "set an alarm for 5:15" is read back as "An alarm at 5:15 am tomorrow, sir?" and waits for a yes instead of committing | jarvis/commander.py — _confirm_or_run(), shaky_transcript(), stash_destructive(), _try_destructive_confirm(), _h_alarm, _h_timer, _h_remind_me | live | "5:15" and "5:50" differ by one phoneme and the cost lands hours later, in the dark. The read-back names the PARSED result (the time Jarvis actually understood, plus "every day" / "weekdays" for a repeat), which is what makes it worth the second. Only the doubtful tail is asked — the threshold is the same -0.7 calibrated for the bulk cancels — so a confident transcript, a typed turn and a CLI turn (no Whisper confidence at all) stay zero-friction. Same machinery as the bulk cancels: the offer expires after DESTRUCTIVE_TTL_S, a "no" or a change of subject drops it, and it can be switched off with `confirm.read_back: false`. Calendar adds are NOT here — add_event has its own read-back (calendar.pending_event) |
